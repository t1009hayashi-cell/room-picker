# 引き継ぎメモ（2026-08-02 時点）

新しいチャットで作業を再開するときは、まずこのファイルと `README.md` を読むこと。
仕様の正は次の3つ。**後のものほど優先する。**
1. `room_assist_requirements.md`
2. `room_assist_additional_requirements.md`（追加要件v1.1）
3. **`プロンプト/room_post_prompt_final.md`（投稿文の型はこれが最新）**

**アプリは投稿文を生成しない。** 投稿文は外部のAIで作って貼り付ける運用。
アプリは「貼った文章がプロンプトの型に沿っているかの判定」と「分析用の記録」を担う。

---

## 1. 現在の状態

**Phase 1〜5 の実装は完了済み。実データで本番稼働している。**

| 項目 | 状態 |
|---|---|
| リポジトリ | https://github.com/t1009hayashi-cell/room-picker （Public） |
| 公開URL | https://t1009hayashi-cell.github.io/room-picker/ |
| 自動実行 | 毎朝 06:00 / 07:00 / 08:00 JST（`.github/workflows/daily.yml`）。2回目以降は取得済みならスキップ |
| ジャンルマスタ | 週1（月 05:00 JST）＋手動（`.github/workflows/genre-master.yml`） |
| テスト | 279件すべて合格（`npm test`。2026-08-08 実測） |
| 初回の実データ取得 | 2026-08-02 に成功。398件取得 / 166件が条件通過 / セール2件を自動検出 |

GitHub Secrets には次の3つが登録済み。

- `RAKUTEN_APPLICATION_ID` … 楽天アプリ管理画面の「アプリケーションID」
- `RAKUTEN_ACCESS_KEY` … 同じく「アクセスキー」
- `RAKUTEN_AFFILIATE_ID` … 任意

---

## 2. 絶対に忘れてはいけない知見（楽天API 2026年刷新）

**仕様書に書かれている旧APIは廃止済み。** ここで長時間ハマったので必ず読むこと。

| | 旧（仕様書の記述・使用不可） | 新（実装で採用） |
|---|---|---|
| ランキング | `app.rakuten.co.jp/services/api/IchibaItem/Ranking/20220601` | `openapi.rakuten.co.jp/`**`ichibaranking`**`/api/IchibaItem/Ranking/20220601` |
| 検索 | `app.rakuten.co.jp/services/api/IchibaItem/Search/20220601` | `openapi.rakuten.co.jp/`**`ichibams`**`/api/IchibaItem/Search/20220601` |
| 認証 | `applicationId` のみ | `applicationId` **と** `accessKey` の両方が必須 |

### 切り分けの落とし穴

- **ランキングと検索でサービスパスが違う**（`ichibaranking` / `ichibams`）。共通だと思うと 404。
- 旧エンドポイントに正しい値を送っても `specify valid applicationId` が返る。
  **でたらめな値を送ったときと完全に同じ応答**なので「IDが間違っている」と誤解しやすい。
  空値で送ると `client_id or access_token is required` になるので、その差で切り分けられる。
- **楽天のテストフォームは楽天サーバー内部から実行される。**
  テストフォームで通っても外部から使える証明にはならない。
  疎通確認はブラウザのアドレス欄にエンドポイントURLを直接入力して行うこと。
- 「許可されたWebサイト / IPアドレス」「アプリケーションタイプ」は今回の原因ではなかった。
  現在は API/バックエンドサービスタイプ + 許可IP `0.0.0.0/0` で登録されているが、
  旧アプリ（Webアプリケーションタイプ）の値でも新エンドポイントなら通る。

この件の反省は `../growth folder/2026-08-02_rakuten-api-legacy-endpoint-misdiagnosis.md` に記録済み。

---

## 3. バグ（次にやること）

実機（iPhone）で確認された3件。いずれも PWA 側（`docs/`）の問題。
**バグ1・3はユーザーが実機で確認し、再発しないことを確認済み（2026-08-03）。**
**バグ2（楽天リンクの着地先）は実機未確認のまま。**

### バグ1: 長押しするとiOSのメニューが出る → 解消（実機確認済み 2026-08-03）

**症状**
カレンダーの日付を長押しすると、iOS Safari の「新規タブで開く」等のコンテキストメニューが出てしまう。

**原因（推定）**
`docs/js/views/calendar.js` に 480ms の長押しで想定報酬合計をポップアップする実装がある
（仕様書8.1「セルを長押しすると、その日の想定報酬合計をポップアップ表示」）。
これが iOS 標準の長押し動作と競合している。
`docs/css/style.css` の `.cal__cell` には `user-select: none` はあるが
**`-webkit-touch-callout: none` が無い**。
加えて、接頭辞なしの `user-select` は **Safari 17 未満では効かない**ため、
古いiOSでは選択メニュー自体も抑止できていなかった。

**対応（2026-08-02）**
- `docs/css/style.css` の `.cal__cell` に `-webkit-touch-callout: none;` と
  `-webkit-user-select: none;` を追加
- `.tabbar__item`（`a` 要素）にも同じ指定を追加
- `docs/js/views/calendar.js` のグリッドで `contextmenu` を `preventDefault()`
- `touch-action` は `.cal__grid` の `pan-y` のままにした。
  `pan-y` は縦スクロールのみ許可する指定で、長押しメニューとは無関係のため。

**意図的に対象外にしたもの**
日別リストの「楽天で開く」（`docs/js/views/dayList.js`）の `a` には
`-webkit-touch-callout: none` を**付けていない**。
バグ2が実機未検証のため、長押しで「リンクをコピー」できる逃げ道を残す判断。
バグ2の解消が確認できたら付けてよい。

**結果**: iPhone実機で再発しないことをユーザーが確認済み（2026-08-03）。

### バグ2: 楽天リンクが商品ページまで飛ばない → 対応済み（要検証）

**症状**
「楽天で開く」を押すと楽天アプリは起動するが、商品ページではなくトップページに着地する。

**判明した原因**
2026年の新APIは `itemUrl` / `shopUrl` を**アフィリエイト転送URL**で返していた。

```
https://hb.afl.rakuten.co.jp/hgc/<アプリID>/?pc=<実URL>&m=<モバイル用URL>
```

楽天アプリがこの転送URLを横取りし、転送先を解決できずトップに落としていた。

**あわせて発覚した重大問題（対応済み）**
この形式は**パス部分にアプリIDが入る**。従来の除去処理はクエリの `rafcid` しか
見ていなかったため素通りし、**公開リポジトリの日次JSONに796箇所アプリIDが露出していた**
（仕様書4.1.2違反）。

**対応（コミット `2d2e265`）**
`src/util/url.ts` の `sanitizeUrl()` を修正。
- 転送URLを検出したら `pc`（無ければ `m`）から実URLを取り出して置き換える
- 取り出せない場合はURL自体を捨てる
- パス・ホスト名にアプリIDが残る場合も捨てる

**検証結果（2026-08-02 実施・解消済み）**

再生成後の `docs/data/2026-08-02.json`（396件）を検査した結果:

| 検査項目 | 結果 |
|---|---|
| `hb.afl.rakuten.co.jp` の残存 | **0件** |
| アプリID（UUID形式）の残存 | **0件** |
| `itemUrl` が直リンク形式でない | **0件** |
| 空の `itemUrl` | 0件 |

`itemUrl` は `https://item.rakuten.co.jp/<shopCode>/<path>/` の形になった。

**次のセッションで確認すること**
- iPhone実機で「楽天で開く」が商品ページに着地するか
- 直っていない場合は、楽天アプリの Universal Links 側の挙動として別途対策を検討

**注意**: アプリIDの除去は仕様書4.1.2の必須要件。除去をやめる方向で解決しないこと。

> ⚠️ **アプリIDの再発行を強く推奨。**
> 露出していた期間は短いが、**Gitの履歴には残る**（コミット `37337a2` の日次JSON、
> および一時的にテストコードにも実値が入っていた）。
> 履歴の書き換えより、楽天側でアプリIDとアクセスキーを再発行し、
> GitHub Secrets を更新する方が確実かつ安全。

### バグ3: ページごとにレイアウトが崩れる → 解消（実機確認済み 2026-08-03）

**症状**
スマホで見たとき、画面によってレイアウトが違う。
カレンダー・分析・設定のタブバーが表示されなかったり、上に表示されたりする。

**原因（推定）**
`docs/css/style.css` の `.tabbar` は `position: fixed; inset: auto 0 0 0;`。
iOS Safari はアドレスバーの伸縮でビューポート高さが動的に変わるため、
`fixed` 要素の位置が不安定になる。
また `.view` の `padding-bottom` が
`calc(var(--tabbar-h) + env(safe-area-inset-bottom) + 24px)` に依存しており、
コンテンツが短い画面（候補0件の日など）で見え方が変わる。

**対応（2026-08-02）**

| やったこと | どの症状に対応するか |
|---|---|
| `body` と `.view` に `min-height: 100dvh`（`100vh` を先に置いて保険） | 内容が短い画面でも高さが確定する |
| `.appbar` / `.tabbar` に不透明の `background: var(--bg)` を先に置いた | `color-mix()` 非対応環境でバーが透明＝「表示されない」を防ぐ |
| `.appbar` / `.tabbar` に `-webkit-backdrop-filter` を追加 | iOS 18未満でぼかしが効かず内容が透ける問題 |
| `docs/js/main.js` の `render()` 先頭で `window.scrollTo(0, 0)` | 画面を切り替えたとき前画面のスクロール位置が残るのを防ぐ |

`position: fixed` は維持した（`sticky` への変更は実機で確認できないため見送り）。

**ローカル検証（Chromium 375×812 で計測）**

| 画面 | 文書高 | タブバー位置 | スクロール位置 |
|---|---|---|---|
| カレンダー | 812px | 画面下端 | 0 |
| 分析 | 1851px | 画面下端 | 0 |
| 設定 | 2987px | 画面下端 | 0 |
| 日別リスト（30件） | 17962px | 画面下端（9000pxスクロール時も固定） | 0 |
| **候補0件の日** | 812px（対応前は内容ぶんのみ） | 画面下端 | 0 |

コンソールエラーなし。`npm test` 126件すべて合格。

**結果**: iPhone実機で再発しないことをユーザーが確認済み（2026-08-03）。
`position: sticky` への変更は不要だった。

---

## 3.5 2026-08-03 に追加した機能

| 追加 | 内容 |
|---|---|
| ジャンルを名前で追加 | 設定画面でジャンル名を打つと候補が出る。`data/genre-master.json` を使う |
| ジャンル別表示 | 日別リストにジャンルの絞り込み（件数付き）を追加 |
| 商品名コピー | 商品カードに「商品名をコピー」。検索用に30文字以内へ整える（下記） |
| 絞り込み2つ | 「ポイント5倍以上」「期間限定価格」を日別リストのチップに追加 |
| 並び順 | 日別リストに hot順／想定報酬順／ポイント倍率順 を追加 |
| 予約投稿 | 投稿文を先に書いて予約し、投稿予定日にコピペする動線（下記） |
| URLコピー | 商品カードに「URLをコピー」「投稿文をコピー」ボタンを追加 |
| 更新の遅延対策 | cronを朝3回に増やし、取得済みならスキップ。設定画面に手動実行の導線 |

### 予約投稿の考え方

**目的**: 商品を見つけた日に投稿文を書いておき、投稿予定日に開いてコピペで投稿する。

動線:

1. 商品カードで投稿文を書く → **「この投稿文で予約する」**
2. カレンダーのその日に **「予○」** のバッジが出る
3. 投稿予定日にその日を開く → **「表示: 予約のみ」** で予約したものだけ出す
4. **「投稿文をコピー」** → 楽天ROOMに貼り付け → **「投稿済みにする」**

決めたこと:

- **投稿済みにすると予約は自動で外れる。** 残すと「予約のみ」に投稿済みが混ざって使えなくなる。
  投稿済みを取り消しても予約は戻らない（必要なら再度予約する）。
- **予約を取り消しても投稿文は消さない。** 書き直す手間を戻さないため。
- 予約時に画面の投稿文をそのまま保存する。`change` イベント待ちだと、
  入力してすぐ予約を押したときに保存漏れが起きうるため。
- **「未投稿を先に」のソートは廃止し、「表示」の絞り込み
  （すべて／予約のみ／未投稿のみ／投稿済みのみ）に置き換えた。**
- **「AI用プロンプト」ボタンは廃止した。**「投稿文をコピー」と役割が重なるため。
  `prompt.js` の `buildAiPrompt()` は残してあるので、戻したくなったら呼び直せばよい。
  投稿ログの `usedAiGeneration` は残しているが、以後は常に false になる。

### 予約・投稿済みは「日付ごと」に記録する（2026-08-03 の修正）

**症状**: 予約や投稿済みを1日で押すと、同じ商品が出ている他の日にも印が付いた。

**原因**: `posted` / `reserved` を **itemCode だけ**をキーにしていた。
発見日モードではランキングに載った日ごとに同じ商品が出るため、全部に反映されていた。

**対応**: キーを `` `${日付}|${itemCode}` `` にした（`store.dayItemKey()`）。

- `isPosted(dateKey, itemCode)` / `undoPost(dateKey, itemCode)` /
  `setReserved(dateKey, itemCode, on, {...})` / `isReserved(dateKey, itemCode)`
- `summarizeDay(items, dateKey, state)` … カレンダーの集計も日付が要る
- 投稿ログに `dateKey`（押したときに見ていた日）を追加した
- **投稿文（`comments`）は itemCode のまま。** 同じ商品なら文面は使い回せるため
- 投稿予定日を変えると、その日に付いていた予約・投稿済みも新しい日へ移す
  （`setScheduledDate(itemCode, 新, 旧)`）。移動元を渡さなければ何も動かさない

**この設計の副作用と、その手当て（2026-08-07）**

日付ごとにしたことで、**別の日で同じ商品を見たときに投稿済みだと分からず、
二重に投稿してしまう**問題が出た（実機で指摘。8/5と8/6には同じ商品が130件出ていた）。

日付ごとの記録は維持したまま、日をまたいだ見え方を足して解決した。

- `store.buildPostedItemIndex()` … 投稿ログから itemCode -> 投稿した日の配列
- `store.postedOnOtherDays(index, itemCode, dateKey)` … その日以外で投稿した日
- カードに **「2026-08-05に投稿済み」** のバッジと、赤い警告
  「この商品は◯◯にすでに投稿しています。ここで投稿すると重ねて出すことになります」
- **「未投稿のみ」「投稿済みのみ」は商品ごとで判定する。**
  日ごとにすると別の日に出た同じ商品が「未投稿」に並び、二重投稿の元になる
- 一覧の上に「この日の候補のうち◯件は別の日にすでに投稿しています」
- カレンダーの「やること」件数も、別の日に投稿済みの商品を数えない
  （数えると同じ商品が複数日に出るぶんだけ水増しされる）

**押した日にだけ印が付く**という元の要件は変えていない。ボタンの状態と投稿ログは
押した日のもの。変えたのは「他の日でも投稿済みだと分かるようにした」ことだけ。

**旧データの移行**: 起動時に自動で振り直す。
投稿済みは投稿ログの `dateKey`（無ければ `postedAt` のJST日付）、
予約は中に持っている `scheduledDate` を使う。すでに新形式のキーは触らない。

### 楽天ジャンル検索API（2026年版・確認済み 2026-08-03）

```
https://openapi.rakuten.co.jp/ichibagt/api/IchibaGenre/Search/20260701
```

- **サービスパスは `ichibagt`。** ランキング(`ichibaranking`)・検索(`ichibams`)のどれとも違う
- 版は `20260701`。旧版 `20170711` はまだ応答するが **2026-08-17 に廃止予定**なので使わない
- レスポンスのキーが商品系と違う: `{ genre, children:[{genreId, nameJa, level}] }`。
  **`genreName` ではなく `nameJa`**。旧版の `current` / `child` / `genreName` 形式も
  `parseGenreResponse()` が受けるようにしてある
- `genreId=0` がツリーのルート
- 取得は第2階層まで。全階層だとリクエスト1秒間隔（仕様書4.3）で終わらない

### 追加要件v1.1 を実装した（2026-08-04）

ターゲットが「共働き世帯・買い物の手間を減らす」から
**「40〜50代・子どもがいる家庭／スーパーでは買えないものを見つけてくる」**に変わった。
仕様は `room_assist_additional_requirements.md`。実装した章と決めたこと。

| 章 | 実装 |
|---|---|
| 1 | レビュー件数 500→**200**、**レビュー平均4.3**の除外を新規追加、新着ブースト |
| 2 | `itemName` / `catchcopy` からクーポン・割引を抽出（`src/discount.ts`） |
| 3 | `pointBoost`（weak/strong/null）。恒常設定は null |
| 4 | `dealScore` を新設。一覧の既定の並び順を `hotScore + dealScore` に |
| 5 | キッチン用品の比率を分析画面に表示。30%超で警告 |
| 6 | 商品ページから `shop_bid` を取って `reviewUrl` を組み立て |
| 7 | **実装しない。** ユーザーがAIに直接聞くため不要との判断（AI用プロンプトのボタンは削除済み） |

**閾値変更の実測（2026-08-04 の実データ341件）**

| 基準 | 通過 |
|---|---|
| 旧（件数500・平均なし） | 158件 |
| **新（件数200・平均4.3）** | **169件** |
| 参考（平均4.2） | 172件 |
| 参考（平均4.0） | 176件 |

**候補は減るどころか増えた。** 件数500→200の緩和が平均4.3の絞り込みを上回る。
要件9章の「減りすぎたら4.2や4.0に緩める」は不要だった。
キッチン用品は169件中26件（15%）で、食品を押し出してもいない。

#### 期限の年の補完は「最も近い未来」にしていない

追加要件2.3は「現在日時から最も近い未来の日付として解釈する」と書いているが、
**それだと8/4に「7/31まで」と書かれた売れ残りの販促文が翌年扱いになり、
4日前に終わった割引を有効なものとして表示してしまう。**
追加要件2.3はもう一方で「期限が過去なら discountRate をリセットして『割引終了』」とも
定めており、未来しか選ばないとこの規則が成立しない。
両方を満たすため **絶対距離で最も近い年**を選んでいる（`resolveDeadline`）。

- 8/4 に「7/31まで」→ 今年の7/31（4日前）→ 割引終了として扱う
- 12/20 に「1/5まで」→ 翌年の1/5（16日先）→ 年跨ぎも正しい（要件9章）

実データで3件が期限切れ（「8/3まで」と書かれた米の商品）として正しく判定された。

#### 誤検出を避けている点

割引率は **`OFF`／`オフ`／`ポイントバック` を必ず伴う場合だけ**拾う。
これを外すと「10%増量」「50%配合」「果汁100%」に反応する（要件9章）。
実データ341件で誤検出0件。割引率の抽出は37件、クーポン36件、期限11件、価格変化4件。

#### 保存済み設定の移行

`load()` が状態を丸ごと保存するため、一度でも起動していれば設定は保存済みで、
`DEFAULT_SETTINGS` を変えても効かない。`meta.settingsVersion` で1度だけ移行する。
**旧既定値のままの項目だけ**を差し替える（自分で変えた値は尊重する）。

#### レビューURLの取得はリクエストが増える

除外を通過した商品ごとに商品ページを1回取る（1秒以上の間隔）。
169件なら約3分、日次バッチの所要時間がその分伸びる。
`maxItems`（既定400）で上限を切ってある。失敗しても `reviewUrl: null` で止めない。
**モック実行では取得しない**（外部にアクセスしないため）。

### 商品名は検索用に短くしてコピーする（2026-08-04）

**楽天の商品名は長すぎてROOMの検索で弾かれる。** 実データ341件で中央値118文字。

`docs/js/lib/itemName.js` の `toSearchQuery()` で30文字以内に整える。
**先頭からN文字で切るだけにしていない。** 楽天の商品名は先頭が販促文であることが多く、
単純に切ると販促文だけをコピーして商品にたどり着けない。

| やること | 例 |
|---|---|
| 先頭の囲み文を落とす（`【】［］＼／`） | `【ふるさと納税】高評価★4.82【…】国産若鶏…` → `国産若鶏バラエティセット 小分け 冷凍 鶏肉` |
| 価格の売り文句を落とす | `お試し送料無料2,490円～ 骨取りサバ切身…` → `骨取りサバ切身 1kg 骨とり 骨なし 鯖 さば` |
| ポイント倍率・期限・送料無料・N冠を落とす | `ポイント5倍 8/3(月)23：59まで ALL令和7年産…` → `ALL令和7年産 きらっとごはん 白米 5kg` |
| `「」`の選択肢注記は落とす。`『』`は残す | `『マダムブリュレ』` は商品名なので残る |
| 語の途中で切らない | 空白区切りで語単位に積む |

**商品の特性を表す語（訳あり・無塩・冷凍・大容量）は落とさない。** 検索の手がかりになる。
販促文を落とした結果「お試し」等の曖昧な語しか残らない場合は、整形後の全体を上限で切る。

実データ341件の結果: 8〜30文字（中央27）、空になるもの0件。
**コピーした内容はトーストに出す。** 短くしている以上、中身が見えないと不安になる。

### UIの修正（2026-08-04・実機のスクリーンショットから）

- **`.btn` に `text-decoration: none` が無く、`a` を使った「楽天で開く」だけ下線が出ていた**
- **操作ボタンを `.actions`（2列グリッド）にした。** flex だと最後の1つだけ次の行に落ちて不揃いに見えた
- **投稿予定日を `.spread` にして右端を揃えた**
- **推奨値の案内を `.badge` から `.hint` に変えた。**
  `.badge` は `white-space: nowrap` なので、文言が伸びると画面外にはみ出していた
- **ハッシュタグを1行目に置いた投稿でタグ数が0と数えられていた。**
  投稿プロンプトは1行目タグを認めているのに、常に警告が出て投稿ログのタグも空になっていた。
  `splitComment()` が先頭のタグ行も見るようにした（タグと本文が混ざった行はタグ行とみなさない）

### 投稿文の生成は廃止した（2026-08-08）

**アプリは投稿文を生成しない。** 運用は「商品を選ぶ → AIにURLを投げる →
出てきた文章を直して投稿」であり、生成した下書きは使われていなかった。

削除したもの:

- `src/comment.ts` と `test/comment.test.ts`（ファイルごと）
- 日次JSONの `draftComments`（**データ量が27%減った**）
- 商品カードの角度タブ、`store.postedAngle`
- `config/scoring.json` の `comment`、`ScoringConfig.comment`、`DraftComment` 型

**残したもの:**

- `docs/js/lib/commentText.js` の `measureComment()` … **貼り付けた文章**が
  プロンプトの型（ヘッダー20〜30字・本文250〜330字・タグ10〜15個など）に
  沿っているかを測る。これは今も編集欄の下に出している
- `docs/js/lib/postFeatures.js` … 分析用の特徴抽出
- `store.comments` … 予約時に保存した投稿文の復元に使う

投稿文欄は**空で始まる**（AIの文章を貼る前提）。

> ⚠️ **「角度」が2種類あった紛らわしさは解消した。**
> 以前は「下書きの角度（コスパ比較・手間削減…）」と「投稿の分類の角度」が併存し、
> 投稿ログには後者だけが入っていた。前者を消したので取り違えは起きない。

### 分析の分類が機能していなかった → 実際の投稿文から取る（2026-08-08）

**症状**: 「採用した文章が出てきたタグと一致せず適当につけている状態」。

**原因**: 運用は「商品を選ぶ → AIにURLを投げる → 出てきた文章を直して投稿」で、
**アプリが生成した投稿文は使っていない。** それなのに投稿ログの `angle` は
アプリ内の角度タブの値を記録していたため、実際の文章と無関係な値が入っていた。

**対策は2つに分けた。**

1. **人にしか分からないものは投稿前に選んでもらう**（`store.postLabels`）
   - ヘッダー型（共感課題型／状況名指し型／数字型／感情語型／対象者明示型／セール速報型）
   - 角度（プロンプトの7種）
   - 選定基準（スーパーにない／評価が高い／今だけ安い・複数可）
   - プロンプトが「案1｜状況名指し × セール速報型」というラベルを出すので、
     **2つ選ぶだけ**で済む形にしている
2. **文章から測れるものは自動で取る**（`docs/js/lib/postFeatures.js`）
   - ヘッダー文字数・本文文字数・タグ込み文字数・行数・1行平均・最長行
   - ハッシュタグ数・箇条書きの行数・CTA・罫線・絵文字・数字・オリジナル写真タグ
   - **貼り付けた実際の投稿文**から取る。生成した下書きではない

投稿ログに `headerType` / `criteria` / `features` / `reviewCountChange` を追加した。
分類が未選択のまま投稿すると、トーストで知らせる（**後から遡って付けられない**ため）。

分析画面の層: 角度別／ヘッダー型別／選定基準別／ヘッダーの文字数帯／
投稿全体の文字数帯／1行あたりの文字数／文章の作り（写真・CTA・箇条書き・罫線）。
**選定基準別だけは合計が投稿数と一致しない**（1投稿が複数の基準に当てはまるため）。

### レビュー件数の増加（2026-08-08）

「いま人気が出ている商品」を見分けるため、前日からのレビュー増加を出す。

- `diff.ts` の `PrevRankIndex` に `reviewCounts` を持たせ、`calcReviewDiff()` で差を取る
- 順位変動と違い**検索API由来の商品でも出せる**（順位を持たなくてよい）
- 前日データが無ければ `null`。0件増と「分からない」を区別する
- カードに `★4.75 / 3,187件 +120` の形で緑色表示。並び順に「レビュー増加順」を追加

### 追加要件v1.3 を実装した（2026-08-22）

`room_assist_requirements_v1_3.md`。

#### 価格下限を 3,000 → 1,000 にした（0章）

プロンプトは「1,000〜3,000円をリーチ枠として6〜7割」と定めているのに、
仕様書側の下限が3,000円のままで、**リーチ枠が除外条件で機械的に全滅していた。**
`config/scoring.json` の `filters.minPrice` を1,000に変更。
アプリ側の設定も `SETTINGS_VERSION` を3に上げ、旧既定値3,000のままなら1,000へ移行する。

#### 比率は「並び順の構造」で担保する（2章）

閾値を下げるだけでは比率は直らない。`dealScore` の「割引率20%以上で+50」が最大の加点で、
**割引率が大きいのは高単価商品**（1,000円が60%OFFになることは稀）だから。
重みで調整すると、その日の割引状況で比率が毎回変わって安定しない。

- `docs/js/lib/pools.js`。価格帯で2プールに分け、`reachRatio` どおりに交互に取り出す
- 判定は「これまで出したreachの割合が目標を下回っていればreachを出す」だけ。
  **端数の場合分けが要らず、どこで打ち切っても比率が保たれる**
- 一方が尽きたら他方で補充し、`reachShort` を立てて「リーチ枠の候補が不足しています」と出す
- **「おすすめ順」だけに適用する。** 「割引率順」を価格帯で崩すと何を見ているか分からなくなる
- `hotScore` の価格加点を `1,000〜3,000円で+15 / 3,000〜25,000円で+10` に変更（2.3）。
  旧実装（5,000〜25,000円で+15）はリーチ枠を加点対象外にしていた
- `reachRatio` は設定画面から変更できる（2.4）。
  **リーチ枠が有利かはまだ実証されていない**（実測では逆の結果）ため、値で切り替えられることが要点

実データ（2026-08-19、110件）での確認: リーチ22件 / 収益88件。
先頭20件のリーチ比率 0.70。リーチ枠が足りず警告が出る状態
（8章が予想したとおり、レビュー200件の条件が低単価で満たされにくい）。

#### 選定理由を日次JSONに出す（3章）

`src/criteria.ts`。`priceTier` / `matchedCriteria` / `criteriaDetail` を各商品に追加。
`confidence` は 確定（API値）/ 推定（商品名から）/ 手動（ユーザーが上書き）。

> **`reason` の文字列を投稿文にそのまま転記しないこと。**
> 「レビュー1,981件」の転記は既存の禁止事項に反する。カードにもその注意を出している。

古い日次JSONには無いフィールドなので、欠けていても落ちないようにしてある
（アプリ側は「この日のデータには入っていません」と出す）。

#### 「スーパーにない」の半自動判定（4章）

`config/supermarket-rules.json` に外出し。大容量・加工済み・業務用・専門店・冷凍の代理指標。
**スーパーの品揃えデータは存在しないので、必ず `推定` にする。**
誤判定は必ず起きるため、商品カードのチェックで手動 on/off できる（`criteriaOverride`）。
「10%増量」「50%配合」を数量と誤認しないことをテストで固定している。

#### 手動での商品追加（5章）— APIからの取得は入れていない

> ⚠ **5.2 手順3（楽天商品検索APIを itemCode で叩く）は実装していない。**
> アプリIDとアクセスキーは公開リポジトリに置けず、静的サイトから叩けば必ず露出する。
> APIを叩けるのはバッチ（GitHub Actions）だけ。
> そのため **5.4 の「最小限の手入力」を正の入口にした。**
> レコードには常に `dataSource: 'manual-input'` が立つ。

- URLからは `itemCode` とショップコードだけを機械的に取り出す（計測パラメータは落とす）
- 除外条件に該当しても除外せず、理由をカードに警告として出す（5.3）
- **手動追加は一覧の先頭に固定する。** スコアを持たない（0点）ので素直に並べると
  最後尾に沈み、「追加したのに出てこない」状態になる（実際に起きた）
- 分析に「手動追加を除く」フィルタ（5.5）。価格帯比率の集計には含める

#### ROOMの投稿画面へ直接飛ばす（追加要望）

**`https://room.rakuten.co.jp/mix?itemcode={shopCode}%3A{itemId}`** が投稿画面。
楽天市場の商品ページにある「ROOMに投稿」ボタンの遷移先で、
`itemcode` はアプリが持っている `itemCode` がそのまま使える。

2026-08-22 に実際に確認した結果:

| URL | 結果 |
|---|---|
| `/mix?itemcode=shop%3Aid` | 200。未ログインだと楽天ログインへ転送され、戻ると投稿画面 |
| `/search/item?keyword=` | 200「ROOM : 検索結果」 |
| `/items/new` | `/all/items`（新着一覧）へ転送。投稿フォームではない |
| `/post` `/add` `/collect` `/collect/item` `/item/new` | すべて404 |

- 「投稿済みにする」→分類を選ぶ→**「投稿文をコピーしてROOMに投稿」**の1タップで
  コピーと遷移が同時に起きる
- **`window.open` は使えない。** `await`（分類ダイアログ）を挟んだあとの `window.open` は
  ユーザー操作の文脈から外れ、iOS Safari のポップアップブロックに掛かる。
  クリップボードも同じ理由で失敗しうる。実体の `<a>` をタップさせる形にしてある
  （`modal.js` の `confirmAction`）
- 楽天のボタンに付く `scid=we_room_upc60` は**付けない**。
  楽天が自社導線を測るための値で、流用すると計測を汚す
- `itemCode` が取れないときだけ ROOM の商品検索に落とす

#### いいね一覧に商品画像（追加要望）

タイトルだけではどの商品か分からないため、56pxのサムネイルを出す。
画像URLは投稿ログに持っていなかったので、
**これからの投稿ログには `imageUrl` / `itemUrl` を残す。**
過去分は読み込み済みカタログから補い、無ければ「—」のプレースホルダを出す。

### 追加要件v1.2 を実装した（2026-08-18）

`room_assist_requirements_v1_2.md`。投稿ログ47件の実測から出た差分。

#### 分類を「ヘッダー型」1軸・4＋1分類に固定した（1章）

分類が入力できていなかった（47件中27件が空欄）直接の原因は、
**プロンプト側の分類名が3回変わり、アプリの選択肢が古い版のまま残っていた**こと。

- **角度は廃止。** `ANGLES` を削除。47件を7分類に割ると1区分数件で、
  「10件未満は数値を出さない」既存方針に照らして最初から分析不能だった
- ヘッダー型を `共感課題型 / お得条件型 / 称号実績型 / 商品特徴型 / 判定不可` に差し替え。
  **この名称は変更しない。**「判定不可」は必須。無いと「近いものを選ぶ」運用になる
- 保存済みの古い型名（状況名指し型・数字型など）は `migrateLabels()` で**落とす**。
  近い名前に読み替えると分析できない偽のデータになるため、対応表は作らない
- **「投稿済みにする」の直前に選択ダイアログを出す**（`docs/js/lib/modal.js`）。
  実際に投稿した1行目を見せて、その場で1タップ。未選択では確定できない
- ヘッダー本文は `headerLine()`（本文の最初の**中身のある行**）を保存する。
  先頭の空行や、1行目に置いたタグ行を拾っていた
- 新規レコードに `labelVersion: 'v2'`、既存には移行時に `'v1'` を立てる。
  **分析は v2 だけで集計する**（`labeledPosts()`）

#### いいね数の手入力（2.1）

ROOMにAPIはなく画面もJavaScript描画のため自動取得できない。
投稿済みのカードに入力欄を出し、`post.likes[]` に `{ count, measuredAt }` で**積む**。
上書きしないのは、投稿直後と1週間後で伸び方が違い初速の判定に使うため。
平均は**記録済みの投稿だけ**で割る（未記録を0とみなすと実態より低く出る）。

#### いいね数は専用の一覧画面から入れる（2026-08-18 追加）

商品カードの中の入力欄だけだと、「投稿した日を思い出して、その日を開いて、
カードを探す」必要があり、**週1回まとめて転記する**という想定運用に合わない。
`#/likes`（`docs/js/views/likes.js`）を追加した。分析画面から入る。

- 投稿を新しい順に並べ、上から数字を埋めて**「まとめて記録」1回**で保存する
- 1回の転記は同じ `measuredAt` で記録する。あとで「いつの計測分か」を追えるようにするため
- 空欄は「今回は入れない」として飛ばす。0との区別が必要なので `''` は保存しない
- 絞り込みは `未記録 / 前回から7日以上 / すべて`。既定は未記録
- 商品URLは投稿ログに持っていないので、読み込み済みカタログから補う。
  見つからなければボタンを出さない（古い商品は日次JSONから落ちるため）

#### 購入済みフラグ（3章）

商品カードのチェック。`state.purchased[itemCode]`。

- **URLをコピーしたときに `【この商品は購入済み。一人称の体験談を書いてよい】` を添える。**
  投稿文の生成を廃止した結果、AIに渡るのはURLだけなので、書き添える先はここしかない。
  URLは1行目に置き、URLだけ使いたいときに壊れないようにしている
- 投稿確定時に `#オリジナル写真` が無ければトーストで促す
- 分析に「購入済みかどうか」の層を追加。買う価値があるかを数字で見るため

#### ハッシュタグ支援（4章）

`docs/js/lib/tagSuggest.js`。商品名からニッチタグを作り、3層に分けて出す。

- 販促文の除去は `itemName.js` の `cleanName()` を共用（新たに export した）
- 文字種の切れ目で語を割り（漢字／ひらがな／カタカナ／英数）、**隣接語をつないだ複合語を優先**。
  「骨取りサバ切身」→ `#骨取りサバ` `#サバ切身`
- 長い候補の一部でしかない語は落とす（`#骨取り` `#サバ` は出さない）
- 数量・配送・販促は除外（`#1kg` `#送料` `#個セット` `#令和` が出ていた）
- **関係ない中規模タグで埋めない。** 商品と無関係なタグが並ぶと選ぶ手が止まる

**タグの記録を本文に一元化した（4.3）。**
`splitComment()` は末尾／先頭の**1行しか**見ておらず、
プロンプトの出力例のようにタグが4〜5行に分かれると2個しか拾えず、
残りは本文に混ざっていた（「タグ数0の行がある」の正体）。連続するタグ行をまとめて切り離す。

#### 要件と実装が食い違っていた点

- **2.2「注文明細CSVのインポートは未実装」は誤り。** `csv.js` / `match.js` /
  設定画面の取り込みUI・`mergeResults()` として実装済み。優先度を上げる作業は不要だった
- 3.2 が前提にしている「AI用プロンプトのコピー」は**すでに削除済み**
  （2026-08-03 に「役割が被る」として投稿文コピーに一本化した）。
  代わりにURLコピーへ注記を足している

### カレンダーをGoogleカレンダー風にした（2026-08-18）

セル内で日付と件数バッジが重なる（8/8 で発生）ため、**積み方そのものを変えた。**

- セルごとの角丸の箱をやめ、**細い罫線で区切ったフラットなグリッド**にした
  （`.cal__grid` の `gap: 0` ＋ `border-top/left`、各セルに `border-right/bottom`）
- 日付は上・中央。今日は `--accent` で塗った丸（`.cal__cell--today .cal__date`）
- 件数は重ならないよう**縦に積む「帯」**にした（`.cal__bar`）
  - セール=赤 / 予約=黄 / 投稿=緑 / 候補=枠線だけ
  - 順番は「セール → 予約 → 投稿 → 候補」。**予約はその日にやることなので投稿済みより先**
- 帯は最大3本。超えたぶんは `他N件`（`.cal__more`）にまとめる
- セル高は `min-height: 92px`。3本＋「他N件」を入れても `overflow` しない値
  （4本入れた状態で `scrollHeight 91 < clientHeight` を実測）

日曜=赤・土曜=青は `data-dow` 属性で当てている。今日の丸を潰さないよう
`:not(.cal__cell--today)` を付けている。

### CSVで書き出せるようにした（2026-08-18）

**設定 > CSVで書き出す** にボタンを置いた。それまで書き出しは JSON だけで、
表計算ソフトで集計する手段が無かった（取り込み側の「注文明細CSVのインポート」と紛らわしい）。

- `docs/js/lib/csvExport.js`。`toCsv(rows, columns)` と列定義（`POST_COLUMNS` 31列 / `RESULT_COLUMNS` 11列）
- **UTF-8 BOM 必須。** 付けないと Excel で日本語が文字化けする
- 改行は CRLF。`=` `+` `-` `@` で始まる値は `'` を前置して**数式として実行されるのを防ぐ**
- 投稿ログには分類（ヘッダー型・角度・選定基準）と、文章から測った特徴を1列ずつ出す。
  表計算側でピボットして層別できるようにするため
- **CSVは読み込めない。** バックアップ用途は従来どおり「JSONをエクスポート」を使う

> 楽天アフィリエイトの「CSVのダウンロードはPCからのみ可能です」は**あちら側の画面の話**で、
> このアプリの書き出しとは無関係。スマホからでも上のボタンで書き出せる。

### 画面の向き（2026-08-08）

`manifest.webmanifest` に `orientation: portrait` は入っているが、
**iOS Safari はこれを無視する。** 回転そのものは止められない。
横向きでも縦向きと同じ体裁になるよう、`@media (orientation: landscape)` で
`.view` を `max-width: 480px` に絞って中央に置いている。
完全に回転させたくない場合は iOS の画面回転ロックを使ってもらう。

### 投稿文の生成を「投稿プロンプト」の型に変えた（2026-08-04）

> ⚠️ **仕様書 9.1 と矛盾している。プロンプト側を正とする。**
> 仕様書は「1行目30〜35文字・全体80〜150文字・3行以内・タグ3〜4個」だが、
> ユーザーが実際にAIへ投げているプロンプトでは
> 「ヘッダー約20文字・ボディ120〜180文字・タグ3〜6個」に変わっている。
> 運用の実態が新しいため、コード側はプロンプトに合わせた。仕様書は未更新。

**プロンプトの原文は `新規 テキスト ドキュメント (2).txt`（リポジトリには入れていない）。**
公開リポジトリにアカウント運用方針を出さないため、意図的に未コミットにしている。
内容を変えたら `src/comment.ts` と `config/scoring.json` の両方を直すこと。

生成する構造（1案あたり）:

| 要素 | 内容 |
|---|---|
| ヘッダー | 16〜24文字。**数字を必ず1つ入れる**。フィードではここしか出ない |
| 結論 | 一番のメリットを1つだけ。複数並べない |
| 理由 | `✅` の箇条書き2〜3項目。**角度ごとに中身を変える** |
| 補足 | スペックの言い切り（容量・単価・送料・レビュー件数） |
| 共感 | 生活場面を1文。体験談は装わない |
| 導入部 | 楽天市場へ送り出す一文 |
| タグ | 3〜6個。**感情・行動タグを必ず1つ** |

決めたこと:

- **文末を「。」で終わらせない。** `dropPeriod()` で落としている
- 角度を6つにした（手間削減／ストック切れ回避／**献立負担**／コスパ比較／季節／**セール速報**）。
  `手間削減` はプロンプトの「時短型」と同じ。既存の投稿ログとの継続性のため名前は変えていない
- **商品名に手がかりが無い角度は選ばない。** 珪藻土バスマットに「献立負担」が付き、
  別商品の紹介文のようになっていたため（`selectAngles` で0点の角度を落とす）
- セール速報は期限（`priceEndTime`）かポイント倍率5倍以上が無いと出さない。
  楽天APIは割引前価格を返さないので、割引率は書けない
- 案の数は `config/scoring.json` の `comment.draftCount`（既定3）。
  増やすと日次JSONが大きくなる
- 文字数帯（`firstLineBand`）を `〜15 / 16〜24 / 25〜` に変更した。
  旧データ（1行目30〜35文字）は「25〜」にまとまる

**カテゴリ語の取り出しを直した。** 商品名の中で**最も早く出てくる語**を選ぶ。
楽天の商品名は後半に検索用キーワードが並ぶため、リストの並び順で選ぶと別物を拾う。
実例: 骨取りさばの商品名後半の「プロテイン」を拾い、
「プロテインならレビュー35,712件」という投稿文になっていた。

**実データでの適合率**: 2026-08-04 の396商品／879案のうち、
基準内が97.6%。外れた21案はすべて文字数不足（90〜119文字、目標120）で、
単価も本数も取れない商品（セール速報の角度）。編集欄に警告が出るので手で足せる。

### ジャンルを追加しても取得対象にならない（2026-08-04 に実際に起きた）

**アプリの「追加」はアプリ内だけの追加。** 楽天から実際に取ってくる対象は
`config/genres.json` で決まる。ここに反映しないと、何度 daily-fetch を回しても
新しいジャンルのデータは出てこない。

対応（2026-08-04）:

- 設定画面の各ジャンルに **「取得中」/「未反映（下の手順が必要）」** のバッジを出す。
  判定は「日次データにそのジャンルが入っていたか」（`genreRows()` の `fetched`）
- 未反映があれば赤い警告でジャンル名を並べる
- 反映手順を ①コピー ②`config/genres.json` を開く ③Commit の3ステップとして明示。
  ②は `https://github.com/<repo>/edit/main/config/genres.json` を直接開く
- ①のコピー内容は `_note` を含むファイル全体にした（丸ごと貼り替える前提のため）

**アプリからリポジトリに直接書き込むことはできない**（書き込み権限のあるトークンが
必要で、公開サイトに置けない）。ユーザーが貼り付ける前提の導線しか作れない。

### GitHub Pages のデプロイが Timeout で落ちることがある（2026-08-06）

コミット `275ced0` のとき、`pages build and deployment` の
**build と Upload artifact は成功したのに deploy だけが `Timeout reached, aborting!` で失敗**した。
GitHub側の一時的な失敗で、コードのプッシュは完了している。

見分け方と対処:

- 症状は「プッシュは通っているのに公開URLのファイルが古いまま」
- `curl -s https://api.github.com/repos/<repo>/actions/runs?per_page=5` で
  `pages build and deployment` の conclusion を見る
- 直し方は**再デプロイを起こすだけ**。GitHubの画面で該当実行の「Re-run failed jobs」を押すか、
  何かコミットを1つ積む（Pagesはmainへのpushで動く）
- **`docs/` の中身は正しいので、コードを直す必要はない**

### GitHub Actions の「再実行」でコミットが失敗する（2026-08-04）

**再実行は最初に実行したときのコミットをチェックアウトする。**
先の試行がすでに push していると、その上に積めず push が拒否される。

2026-08-04 の実行#19（手動・5回再実行）の内訳:

| 試行 | 結果 | 落ちたステップ |
|---|---|---|
| 1 | 失敗 | 日次JSONを生成（10秒で落ちた。正常は約15秒） |
| 2 | 失敗 | 日次JSONを生成（3秒で落ちた） |
| 3 | **成功** | — （コミット `510ba26`） |
| 4 | 失敗 | コミット（push 拒否） |
| 5 | 失敗 | コミット（push 拒否） |

対応: コミットステップで push が拒否されたら `git fetch` + `git rebase` して
最大3回まで積み直す（daily.yml / genre-master.yml の両方）。

試行1・2の失敗はログを読めず（`Must have admin rights to Repository.`）
原因を断定できていない。短時間に5回×約14リクエストを投げているため
楽天側の 429 が有力。念のため 429・408・5xx・通信エラーを
`RakutenClient` で最大3回リトライするようにした（`Retry-After` があれば従う）。
**認証エラー(400)と404はリトライしない**（待っても変わらないため）。

ログは `curl` では取れない（管理者権限が必要）。ユーザーにGitHubの画面で
該当ステップを開いてもらうしかない。

### ジャンルマスタが未生成のときの挙動

`docs/data/genre-master.json` が無い間は、設定画面は従来どおり genreId の手入力に戻る。
日次バッチにも「無ければ作る」ステップを入れてあるので、翌朝の自動実行で用意される。
すぐ欲しい場合は GitHub で `genre-master` ワークフローを手動実行する。

### 「割引中」について

**楽天APIは割引前の価格を返さない。** そのため値引き額は出せない。
`priceStartTime` / `priceEndTime`（期間を切って出している価格）を「期間限定価格」として扱っている。
2026-08-02 のデータでは396件中38件が該当。
前日比の値下がりで判定する案もあるが、2日分以上のデータが必要なため未実装。

## 4. 補足

- **現在公開中のデータは、投稿文の修正（コミット `bae766f`）より前に生成されたもの。**
  珪藻土バスマットの投稿文が「水の買い出しに使う時間…」になっているのはそのため。
  翌朝の自動実行で正しい内容に置き換わる。
- 順位変動（↑↓）は前日データが必要なため、2日目以降から表示される。
- 実機の画面では投稿文が「本文 79文字」で推奨範囲（80〜150）を下回り警告が出ていた。
  カテゴリ語の変更で文字数が変わるため、修正後のデータで再確認すること。
- ユーザーはプログラミングの専門知識を前提としない説明を求めている。
  専門用語を避け、操作は具体的な手順で案内すること。
- 認証情報（アプリID・アクセスキー）は絶対にコードやリポジトリに書かない。
  ユーザーにも値をチャットに貼らないよう案内すること。

# 楽天ROOM 投稿支援ツール 追加要件（v1.3）

既存実装への差分のみ。v1.1・v1.2 の記述と食い違う箇所は**本書を正とする。**

---

## 0. v1.1 の誤りの訂正（最重要）

v1.1 の「1. 除外条件の閾値変更」の表に、次の誤りがあった。

| 項目 | v1.1の記載（誤） | 正 |
|---|---|---|
| 価格の下限 | 3,000（据え置き） | **1,000** |

投稿文生成プロンプトでは「1,000〜3,000円をリーチ枠として6〜7割」と定めているが、
仕様書側が下限3,000円のままだったため、**リーチ枠の商品が除外条件で
機械的に落とされていた。** 実投稿51件で価格中央値4,180円、
1,000〜3,000円が12件しかなかったのは、この不整合が直接の原因である。

**まず価格下限を 3,000 → 1,000 に変更すること。**

ただし後述のとおり、**閾値変更だけでは比率は改善しない。**
2章の候補プール分割と併せて実装する必要がある。

---

## 1. 現行ロジックが高単価に偏る理由

### 1.1 構造上の原因

現在の一覧ソートは `hotScore + dealScore` の降順である。

```
dealScore =
    discountRate >= 20 なら +50
  + hasCoupon なら +30
  + pointRate >= 5 なら +20
  + pointRate 3〜4 なら +5
  + セール期間中なら +15
```

`discountRate >= 20` の +50 が最大の加点になっている。
そして**割引率が大きいのは高単価商品である。**
1,000円の商品が60%OFFになることは稀だが、
10,000円の商品が半額になるのは日常的に起きる。

さらに `hotScore` にも
「価格が5,000〜25,000円なら +15」という加点が含まれている。
**リーチ枠（1,000〜3,000円）はこの加点の対象外**であり、
二重に不利になっている。

### 1.2 決定的な原因

**価格帯による出力比率の制御が、どこにも実装されていない。**
スコア上位を単純に並べているだけなので、
スコアが高単価に偏れば出力もそのまま偏る。

比率をスコアの重み調整で制御しようとすると、
その日の割引状況によって毎回比率が変わり、安定しない。

---

## 2. 価格帯別の候補プール分割（推奨案）

### 2.1 検討した選択肢

| 案 | 内容 | 実装コスト | 比率の制御 | 判定 |
|---|---|---|---|---|
| A | 価格下限を1,000円に変更するだけ | 極小 | できない | 不十分 |
| B | dealScoreの重みを下げる | 小 | 間接的・不安定 | 却下 |
| C | **価格帯別に候補プールを分ける** | **中** | **確実** | **推奨** |
| D | 低単価にスコア加点を追加 | 小 | 調整が難しい | 却下 |

**比率を「スコアの結果」ではなく「出力の構造」で担保する**のがC案の要点。

### 2.2 実装仕様

```
1. 除外条件の価格下限を 1,000 に変更する

2. 除外条件を通過した候補を、価格で2つのプールに分割する
   - reachPool  : itemPrice が 1,000〜3,000円
   - revenuePool: itemPrice が 3,000円超
   ※ hasPriceRange が true の商品は itemPriceMax で判定する
     （既存仕様どおり。除外判定は最大価格、報酬見積もりは最小価格）

3. 各プール内で hotScore + dealScore の降順にソートする

4. 出力リストを構造的に組む
   表示件数を N とし、reach:revenue = 7:3 で配分する
   例）N=10 → reachPool から7件、revenuePool から3件
   並び順は、両プールから交互に取り出す
   （reach, reach, revenue, reach, reach, revenue, ...）

5. 一方のプールが枯渇した場合のみ、他方から補充する
   補充した場合は、UIに「リーチ枠の候補が不足しています」と表示する
```

### 2.3 hotScore の価格加点を修正

現行の「価格が5,000〜25,000円なら +15」を、次に変更する。

```
+ 価格が 1,000〜3,000円なら +15    // リーチ枠
+ 価格が 3,000〜25,000円なら +10   // 収益枠
```

プール分割で比率は担保されるため、この加点は各プール内の
順序付けにしか影響しない。**過度な調整は不要**だが、
リーチ枠が不当に低く評価される状態は解消しておく。

### 2.4 配分比率を設定可能にする

`config` に `reachRatio` を追加し、初期値 0.7 とする。
**検証期間の運用方針が変わった際に、コードを触らず調整できるようにする。**

なお現在（2026/8/19時点）、実測ではリーチ枠のほうが
1日あたりいいねが低いという逆の結果が出ているが、
サンプル不足と時期の交絡により判定できていない。
**設定値で切り替えられる形にしておくことが重要である。**

---

## 3. 選定理由の出力（新規）

### 3.1 背景

現在、除外条件の判定は行っているが、
**「どの基準で選ばれたか」が出力に残っていない。**
投稿文生成側のAIが選定理由を正確に本文へ反映するために必要。

### 3.2 実装

日次JSONの各商品に、次を追加する。

```json
{
  "priceTier": "reach",
  "matchedCriteria": ["評価が高い", "今だけ安い"],
  "criteriaDetail": {
    "スーパーにない": {
      "matched": true,
      "reason": "2kgの大容量・骨取り加工済み",
      "confidence": "推定"
    },
    "評価が高い": {
      "matched": true,
      "reason": "レビュー1,981件・評価4.52",
      "confidence": "確定"
    },
    "今だけ安い": {
      "matched": true,
      "reason": "50%OFF・8/22 23:59まで",
      "confidence": "確定"
    }
  }
}
```

- `matched` が1つもない商品は候補に出さない
- `confidence` は「確定」（API値による判定）か「推定」
  （商品名からの推定。後述のスーパー判定が該当）を区別する
- **`reason` の文字列を投稿文にそのまま転記しないこと。**
  AIが選定理由を理解するための情報であり、
  「レビュー1,981件」等をそのまま書くのは既存の禁止事項に反する

### 3.3 AI用プロンプトのコピー内容に追加

```
価格帯：リーチ枠（1,000〜3,000円）
該当した選定基準：評価が高い／今だけ安い
　- 評価が高い：レビュー1,981件・評価4.52（確定）
　- 今だけ安い：50%OFF・8/22 23:59まで（確定）
```

---

## 4. 「スーパーにない」の半自動判定（新規）

### 4.1 前提

**完全な自動化は不可能。** スーパーの品揃えデータが存在しないため。
以下は代理指標による近似であり、`confidence: "推定"` として扱う。

### 4.2 判定ロジック

`itemName` と `catchcopy` から、次のいずれかに該当すれば `matched: true`。

| 観点 | 抽出パターン | 例 |
|---|---|---|
| 大容量 | `\d+\s*kg`、`\d+\s*本入`、`\d+\s*袋`、`\d+\s*食`、`\d+\s*個入` で数量が閾値以上 | 2kg、50本入 |
| 加工済み | `骨取り`、`カット済`、`小分け`、`皮なし`、`下処理` | 骨取りサバ |
| 業務用 | `業務用`、`メガ盛り`、`大容量`、`まとめ買い` | 業務用フランク |
| 専門店・産地 | `産地直送`、`〇〇直送`、`専門店`、`自家焙煎` | 自家焙煎コーヒー |
| 冷凍 | `冷凍便`、`冷凍配送` | — |

閾値の目安：1kg以上／20本以上／10食以上。
**この閾値は運用しながら調整するため、`config/supermarket-rules.json` に外出しする。**

### 4.3 手動修正を可能にする

推定である以上、誤判定は必ず起きる。
**商品カードに「スーパーにない」のチェックボックスを置き、
手動で on/off できるようにする。**
手動で変更した場合は `confidence: "手動"` に書き換える。

---

## 5. 手動での商品追加（新規）

### 5.1 背景

ソフトの候補一覧に出てこない商品を投稿したい場面がある。
現在はその商品の投稿ログが残らず、**分析対象から漏れる。**
実際、Perplexityで生成した投稿がCSVに含まれていない事例が発生している。

分析の前提は「投稿した全件が記録されていること」であり、
一部が欠けると比率も平均も正しく出せない。

### 5.2 実装

一覧画面に「**商品を手動で追加**」ボタンを設置する。

**入力方法は楽天の商品URLの貼り付けのみとする。**
手入力の項目を増やすと、記録されない項目が発生して分析に穴が開く。

```
1. ユーザーが商品URLを貼る
2. 商品URLから itemCode を抽出する
   （形式：https://item.rakuten.co.jp/{shopCode}/{itemId}/）
3. 楽天商品検索API（IchibaItem/Search）を itemCode で叩き、
   通常の候補と同じ構造のデータを取得する
   ※ ランキングAPIではなく検索APIを使う。
     ランキング外の商品は前者では取得できないため
4. 取得したデータに対し、通常と同じ判定を行う
   - 除外条件の判定（ただし除外はせず、結果のみ記録する）
   - matchedCriteria の算出
   - priceTier の判定
   - hotScore / dealScore の算出
5. `source: "manual"` を立てて候補一覧に追加する
6. rank / prevRank / rankChange は null とする
```

### 5.3 除外条件に該当する商品の扱い

**手動追加した商品は、除外条件に該当しても除外しない。**
ユーザーが意図して選んだものであるため、判断を上書きしない。

ただし、該当した除外理由をカード上に警告として表示する。

```
⚠ 通常の抽出条件では除外される商品です
　・レビュー163件（基準200件未満）
　・「スーパーにない／評価が高い／今だけ安い」のいずれにも該当しません
```

### 5.4 API取得に失敗した場合

商品が販売終了している、URLの形式が想定外である等の理由で
取得に失敗することがある。この場合は**最小限の手入力**を許可する。

必須：商品名、価格、ショップ名、商品URL
任意：レビュー件数、レビュー平均

手入力で作成したレコードには `dataSource: "manual-input"` を立て、
集計時に「API取得分」と区別できるようにする。
**手入力分は選定ロジックの検証には使わない**（データの質が異なるため）。

### 5.5 集計上の扱い

- `source: "manual"` の投稿も、いいね数・分類・成果の記録対象とする
- 価格帯比率の集計には**含める**
  （手動追加が高単価に偏っていれば、それも実態として把握すべきであるため）
- 分析画面で「手動追加分を除く」フィルタを用意する

---

## 6. 互換性の確認

| 変更 | 既存機能への影響 |
|---|---|
| 価格下限 3,000→1,000 | なし。除外条件の閾値のみ |
| 候補プール分割 | なし。ソートと表示順のみ。データ構造は不変 |
| hotScoreの価格加点 | 各プール内の順序が変わる。過去データの再計算は不要 |
| matchedCriteria追加 | 既存フィールドは変更せず追加のみ。後方互換 |
| スーパー判定 | 新規フィールドの追加のみ |
| 手動追加 | `source` フィールドの追加のみ。既存レコードは `"ranking"` 扱い |

**レビュー判定・クーポン抽出・レビューURL自動生成には影響しない。**

日次JSONのスキーマは追加のみで、既存フィールドの削除・改名は行わない。
過去の日次JSONは、新フィールドが欠けた状態でも読み込めるようにすること
（欠損時は `null` として扱う）。

---

## 7. 実装順序

| 優先 | 項目 | 理由 |
|---|---|---|
| 1 | 0章（価格下限 3,000→1,000） | 1行の変更。これがないと候補が存在しない |
| 2 | 2章（候補プール分割・比率制御） | 比率問題の本質。ここで解決する |
| 3 | 5章（手動追加） | 分析データの欠落を止める。早いほど良い |
| 4 | 3章（選定理由の出力） | 投稿文の質に直結 |
| 5 | 4章（スーパー判定） | 精度が出にくいため最後 |

**1と2は同時に入れること。** 1だけでは候補が増えるだけで比率は偏ったままになる。

---

## 8. 実装後に確認すること

- 価格下限を1,000円にした結果、リーチ枠の候補が実際に何件出るか
　→ 極端に少ない場合、レビュー件数200件の条件が
　　 低単価商品で満たされにくい可能性がある。その場合は
　　 リーチ枠に限りレビュー件数の閾値を下げることを検討する
- 7:3の配分で、リーチ枠が枯渇する日がどの程度あるか
- 手動追加した商品のAPI取得成功率
- `matchedCriteria` が空になる商品が、候補から正しく除かれているか
- スーパー判定の誤検出（「#1kg」のような無意味な抽出が出ていないか）

---

## 9. 未確認事項

- **リーチ枠が本当に有利なのかは、まだ実証されていない。**
　2026/8/19時点の実測では、1日あたりいいねはリーチ枠0.35、
　3,000円超1.32と逆の結果が出ている。
　ただしサンプル不足（リーチ枠12件）と時期の交絡により判定不能。
　`reachRatio` を設定値にしてあるのは、この検証結果によって
　方針を変更する可能性があるためである。
- 楽天商品検索APIを `itemCode` 指定で叩いた場合、
　ランキングAPIと同一のフィールドが返るかは要検証。
　特に `affiliateRate` `pointRate` の有無を確認すること。

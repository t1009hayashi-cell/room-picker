# 楽天ROOM 投稿支援ツール 追加要件（v1.1）

既存実装への差分のみを記載する。
背景：アカウントのターゲットと選定基準を変更したため、抽出条件を合わせる必要がある。

## 0. 変更の背景

**旧：** 共働き世帯向け。「買い物の手間を減らす」が軸。
**新：** 40〜50代・子どもがいる家庭向け。**「スーパーでは買えないものを見つけてくる」**が軸。

この層は車を持っている場合が多く、「重い・買いに行くのが面倒」は購入動機として弱い。
ネットで食品を買う動機は次の3つに絞られる、という前提に立つ。

1. スーパーに売っていない
2. 評判がよく、周囲から勧められた
3. クーポンやセールで明確にお得

抽出条件もこの3つに対応させる。

---

## 1. 除外条件の閾値変更

`config` の該当値を変更する。

| 項目 | 現行 | 変更後 | 理由 |
|---|---|---|---|
| レビュー件数の下限 | 500 | **200** | 500だと定番数商品しか残らず、顔ぶれが固定化する |
| レビュー平均の下限 | （未実装） | **4.3** | **新規追加。** 実質の品質判定はこちらで行う |
| 価格の下限 | 3,000 | 3,000（据え置き） | — |

### 1.1 `reviewAverage` による除外を新規実装

現在 `reviewAverage` は取得しているが判定に使っていない。除外条件に追加する。

```
除外： reviewAverage < 4.3
```

実データ（コーヒー30件）での検証：
- レビュー1,571件・評価4.16の商品が存在した。件数は十分だが評価が低く、実際に不満が出ているサイン
- 4.3を下限にするとこの種を弾ける

### 1.2 新着ブースト（例外規則）

次の条件を満たす商品は、`reviewAverage` の条件を**免除**する。

```
reviewCount >= 50 かつ rankChange >= 3
```

注目され始めた商品はレビューが溜まっていないだけで、定番商品より投稿の希少価値が高い。
免除した商品には `newcomerExempt: true` を立て、UI上でバッジ表示する。

---

## 2. クーポン・割引情報の抽出（新規機能）

### 2.1 前提

**楽天ウェブサービスにクーポン専用APIは存在しない。**
提供されているのは楽天市場・ブックス・トラベル・レシピ・Kobo・GORAのみ。

ただし**店舗が集客のため `itemName` と `catchcopy` に書き込んでいる**ため、
テキストから抽出できる。実データで確認済みの例：

- `【10%OFFクーポン対象 7/31 09:59まで】コカ・コーラ ジョージア...`
- `【15%OFFクーポンご利用で4,488円！1本あたり94円以下！7/31 23:59まで】...`
- `【今すぐ使える！店内全品45％OFF】...`
- `【 7/31 23:59まで 57%OFF + 先着クーポン配布中 】...`
- `いまだけ＼半額 50%OFF／...`
- `【4,620円→2,992円 +エントリーでP5倍】【35%OFF SALE】...`

### 2.2 抽出仕様

`itemName` と `catchcopy` の**両方**を対象に、以下を抽出する。

```
割引率：      (\d{1,2})\s*[%％]\s*(OFF|オフ|off|ポイントバック)
半額表記：    半額            → discountRate = 50 として扱う
クーポン有無： クーポン
期限：        (\d{1,2})\s*[\/月]\s*(\d{1,2})\s*日?\s*(\d{1,2}:\d{2})?\s*まで
価格変化：    ([\d,]+)円\s*[→⇒]\s*([\d,]+)円
```

抽出結果を日次JSONに追加する。

```json
{
  "discountRate": 60,
  "hasCoupon": true,
  "couponDeadlineRaw": "8/3 23:59まで",
  "couponDeadline": "2026-08-03T23:59:00+09:00",
  "priceBefore": 4620,
  "priceAfter": 2992,
  "extractedFrom": "itemName"
}
```

### 2.3 抽出時の注意

- **全角％と半角%の両方**に対応すること。実データに両方存在した
- 期限の年は記載されないため、**現在日時から最も近い未来の日付**として解釈する
- 抽出できなかった項目は `null`。推測して埋めない
- **抽出した文言を投稿文に自動転記しない。** 数値としてのみ保持する
　（「最安値に挑戦」等が混入すると景表法上の表示責任を負うため）
- 期限が過去の場合は `discountRate` を `null` にリセットし、
　UIに「割引終了」と表示する

---

## 3. ポイント倍率の扱い変更

### 3.1 現行の問題

現行は `pointRate >= 5` を優先条件にしている。
実データを見ると、`pointRate: 2` は澤井珈琲のように
**`pointRateEndTime` が `9999-12-31 23:59` の恒常設定**であり、セールではない。

一方、5倍以上は期限付きの実キャンペーンだった。

### 3.2 変更内容

閾値を3に下げるが、**「候補に入れる」と「訴求してよい」を分離する。**

| pointRate | 扱い | フラグ |
|---|---|---|
| 1〜2 | 候補に入れるが加点しない | — |
| 3〜4 | 候補に入れる。**投稿文で数字を出さない** | `pointBoost: "weak"` |
| 5以上 | 加点する。**投稿文で訴求してよい** | `pointBoost: "strong"` |

**`pointRateEndTime` が `9999-12-31` の商品は、倍率に関係なく `pointBoost: null` とする。**
恒常設定であり「今だけ」ではないため。

5,000円の商品で3倍だと上乗せは約100円分にとどまり、
「お得」と言い切るには弱い。母数確保のために候補には入れるが、訴求材料にはしない。

---

## 4. 優先順位の再定義

`hotScore` とは別に、**「今だけ安い」の強さを表す `dealScore`** を新設する。

```
dealScore =
    discountRate >= 20 なら +50        // 割引が最も強い訴求
  + hasCoupon なら +30
  + pointRate >= 5 なら +20
  + pointRate 3〜4 なら +5
  + セール期間中（sales.json と照合）なら +15
```

**60%OFFはポイント20倍より圧倒的に強い訴求である**ため、割引率を最優先とする。

一覧のソートは `hotScore + dealScore` の降順とし、
絞り込みチップに「**割引あり**」「**クーポンあり**」を追加する。

---

## 5. 対象ジャンルの追加

`config/genres.json` に追加する。

| ジャンル | genreId | 料率 | 位置づけ |
|---|---|---|---|
| キッチン用品・食器 | 558944 | 3% | **従。全投稿の2〜3割まで** |

### 5.1 注意事項

- **調理家電は追加しない。** 家電ジャンルは料率2%で報酬効率が最も悪い
- キッチン用品は耐久財でリピートしないため、主力にはしない
- 扱う範囲は「食卓まわり」に限定（保存容器・鍋・調理器具・弁当箱・水筒・食器）。
　掃除用品や収納は対象外
- **UIに、直近30日の投稿に占めるキッチン用品の比率を表示する。**
　30%を超えたら警告を出す（食品アカウントとしての一貫性を保つため）

---

## 6. レビューURL情報の保持（新規）

投稿文生成時にレビューを参照するため、レビューページのURLを組み立てられるようにする。

### 6.1 背景

商品ページはEUC-JPで、レビュー本文が文字化けして読めない場合がある。
一方、レビュー専用ページはUTF-8で正常に取得でき、
**AI要約・トピックタグ・用途タグ・レビュアー年代**まで取得できる。

```
https://review.rakuten.co.jp/item/1/{shopBid}_{itemId}/1.1/
```

### 6.2 実装

`shopBid`（ショップの数値ID）はAPIレスポンスに含まれない。
商品ページのHTML内に `shop_bid=249917&iid=10001714` という形で存在するため、
**商品ページを取得して正規表現で抽出する。**
文字化けしていてもこの部分はASCIIなので読み取れる。

日次JSONに追加：

```json
{
  "shopBid": "249917",
  "itemNumericId": "10001714",
  "reviewUrl": "https://review.rakuten.co.jp/item/1/249917_10001714/1.1/"
}
```

**全商品で取得するとリクエスト数が膨らむため、
除外条件を通過した商品のみを対象**とし、1秒以上の間隔を空けること。

取得に失敗した場合は `reviewUrl: null` とし、エラーで止めない。

---

## 7. AI用プロンプトのコピー内容を更新

「AI用プロンプトをコピー」で出力するテキストを、新しい選定基準に合わせる。

```
以下の商品について、楽天ROOMの投稿コメントを5案作ってください。

商品名：{itemName}
価格：{itemPrice}円{priceBefore→priceAfter があれば併記}
ショップ：{shopName}
レビュー：{reviewCount}件（平均{reviewAverage}）
割引：{discountRate}% / クーポン：{hasCoupon} / 期限：{couponDeadlineRaw}
ポイント：{pointRate}倍（{pointBoost}）
レビューページ：{reviewUrl}
商品説明：{itemCaptionShort}

ターゲット：40〜50代・子どもがいる家庭（主に母親）
選定基準：スーパーにない／評価が高い／今だけ安い
この商品が該当する基準：{matchedCriteria}
```

`reviewUrl` を含めることで、AI側がレビューを取得して投稿文に反映できる。

---

## 8. 実装順序の推奨

| 優先 | 項目 | 理由 |
|---|---|---|
| 1 | 1章（閾値変更・reviewAverage判定） | 設定値の変更のみ。すぐ効果が出る |
| 2 | 7章（プロンプト更新） | 文字列の差し替えのみ |
| 3 | 5章（ジャンル追加） | 設定ファイルへの追記のみ |
| 4 | 2章（クーポン抽出） | 新規実装。正規表現の検証が必要 |
| 5 | 3・4章（dealScore） | 2章の完了が前提 |
| 6 | 6章（レビューURL） | リクエスト数が増えるため最後 |

1〜3は設定変更のみで完了する。**まずここまでを反映して運用を始め、
4以降は実際の投稿を数十件こなしてから着手しても遅くない。**

---

## 9. 検証時に確認すること

- クーポン抽出の正規表現が、実際の商品名で誤検出しないか
　（「10%増量」「50%配合」など割引以外の数値％に反応しないか）
- 期限の年跨ぎ（12月に「1/5まで」と書かれた場合）が正しく解釈されるか
- `reviewAverage` 4.3の閾値で、候補が極端に減っていないか
　→ 減りすぎた場合は4.2または4.0まで緩める
- キッチン用品を追加したことで、食品の候補が押し出されていないか
　→ ジャンルごとに表示上限を設ける必要があるか判断する

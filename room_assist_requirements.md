# 楽天ROOM 投稿支援ツール 要件定義書

## 1. 目的

楽天市場のランキングから、楽天ROOMに投稿する商品候補を毎日自動抽出し、
iPhoneのカレンダーUIから候補の確認・投稿文の管理・投稿予定日の割当を行う。

外出先（通勤中など）にiPhoneだけで完結することを最優先とする。
PCの電源が入っている必要がない構成にすること。

## 2. 全体構成

```
GitHub Actions（毎朝 06:00 JST）
  ├─ 楽天市場ランキングAPI を叩く（ジャンル別）
  ├─ 前日のJSONと突き合わせて順位変動を計算
  ├─ 除外条件でフィルタ
  ├─ 投稿文テンプレートを生成
  └─ data/YYYY-MM-DD.json をリポジトリにコミット
        ↓
GitHub Pages（静的ホスティング）
  ├─ / （PWA本体：HTML + JS + CSS）
  └─ /data/*.json
        ↓
iPhone Safari →「ホーム画面に追加」でPWAとして常駐
```

### この構成を選ぶ理由

- **CORSが発生しない。** ブラウザから楽天APIを直接呼ぶとCORSエラーになるため、
  API呼び出しはActions側（サーバーサイド）で行い、ブラウザは同一オリジンの静的JSONを読むだけにする。
- **アプリIDがクライアントに露出しない。** GitHub Secretsに格納する。
- **日次JSONがそのまま履歴になる。** 順位変動＝「hot」の判定に前日データが必須。
- **App Store申請・署名が不要。** ホーム画面に追加するだけ。
- **全て無料枠に収まる。**

## 3. 技術スタック

| 層 | 採用 | 備考 |
|---|---|---|
| 定期実行 | GitHub Actions（schedule: cron） | UTC指定なので `0 21 * * *` = JST 06:00 |
| 抽出スクリプト | Node.js (TypeScript) または Python | どちらでも可。Actionsで動けばよい |
| ホスティング | GitHub Pages | プライベートリポジトリの場合は要確認 |
| フロント | 素のHTML/CSS/JS、またはVite + React | ビルドが必要ならActionsでビルドしてPages公開 |
| ローカル保存 | localStorage または IndexedDB | 投稿予定日・編集済み投稿文・投稿済みフラグ |

**注意：リポジトリを公開にするか非公開にするかを先に決めること。**
非公開の場合、GitHub Pagesの利用可否とデータJSONへのアクセス方法が変わる。
公開にする場合、抽出結果が第三者から見えることを許容できるか判断すること。

## 4. データ取得

### 4.1 API

楽天ウェブサービス「楽天市場ランキングAPI」を使用する。

- エンドポイント: `https://app.rakuten.co.jp/services/api/IchibaItem/Ranking/20220601`
- 必須パラメータ: `applicationId`, `format=json`
- 主要パラメータ: `genreId`, `page`
- 任意: `affiliateId`（指定すると `affiliateUrl` が返る）

**実レスポンスで確認済み（2026-07-31時点）。以下は実測に基づく確定情報。**

**取得できるフィールド（すべてランキングAPIで確認済み）**

| フィールド | 型 | 用途・注意 |
|---|---|---|
| `itemCode` `itemName` `shopName` `shopCode` | string | 基本情報・突合キー |
| `itemPrice` | **string** | `"8360"` のように文字列。要パース |
| `itemPriceMin1〜3` `itemPriceMax1〜3` `hasPriceRange` | string / int | 価格帯商品あり。`hasPriceRange:1` なら幅がある |
| `itemCaption` | string | **数千文字に達する。保存時は切り詰め必須** |
| `catchcopy` | string | ショップの宣伝文。**投稿文に転記しないこと** |
| `reviewCount` | int | そのまま使える |
| `reviewAverage` | **string** | `"4.83"`。要パース |
| **`affiliateRate`** | **string** | **`"4.0"` = 4%。パーセント単位。100で割ること** |
| `postageFlag` | int | **0 = 送料込み / 1 = 送料別。値の意味が直感と逆なので注意** |
| `availability` | int | 0 or 1 |
| **`pointRate`** | int | ポイント倍率。`20` など |
| **`pointRateStartTime` `pointRateEndTime`** | string | **ポイント倍率の適用期間。セール検出に使う（5.2参照）** |
| `startTime` `endTime` | string | **その価格の有効期限。空文字の場合あり** |
| `shopOfTheYearFlag` | int | 受賞店フラグ |
| `rank` | int | 順位 |
| `genreId` | string | **商品自身のジャンル。リクエストしたgenreIdとは別物** |
| `itemUrl` `shopUrl` | string | **末尾に `?rafcid=wsc_i_ra_<アプリID>` が付与される（後述）** |
| `mediumImageUrls` `smallImageUrls` | array | `[{imageUrl: "..."}]` の形 |
| `affiliateUrl` `shopAffiliateUrl` | string | affiliateId未指定時は空文字 |

**型が混在している点に注意。** 価格・料率・レビュー平均は文字列、レビュー件数・在庫・ポイント倍率は数値で返る。
**すべて明示的にパースすること。** 文字列のまま比較すると `"10000" < "9000"` のような誤判定が起きる。

**返却件数は30件。** 配列は `rank` の降順（30位が先頭、1位が末尾）で並んでいた。
**配列の順序を順位とみなさず、必ず `rank` でソートし直すこと。**

`formatVersion=2` を指定すると `Item` のネストが外れて扱いやすくなる。実装時に検証すること。

### 4.1.1 ジャンル指定

`genreId` は正しく機能することを確認済み。
テストフォームでは「パラメータ」欄に指定する。指定しない場合は総合ランキングが返る。

レスポンスの `title` にジャンル名が含まれる（例：ランキング市場【コーヒー】）。
**取得時にこの文字列を検査し、期待したジャンルでなければ警告を出すこと。**
指定漏れに気づかないまま総合ランキングを取り込むと、まったく無関係な商品が混入する。

### 4.1.2 アプリIDの露出（要対処）

`itemUrl` と `shopUrl` の末尾に `?rafcid=wsc_i_ra_<アプリID>` が自動付与される。
**このままリポジトリにコミットすると、パブリックリポジトリ上にアプリIDが公開される。**

シークレットではないため即座に実害はないが、第三者にQPSを消費される可能性がある。
**保存前に `rafcid` パラメータを除去すること。** URLからクエリ文字列を落とすだけでよい。

### 4.2 商品検索APIの併用

ランキングAPIのみでは上位が固定化し、同じ商品しか出てこない。
`IchibaItem/Search` を併用して母集団を広げる。

- エンドポイント: `https://app.rakuten.co.jp/services/api/IchibaItem/Search/20220601`
- 有効なパラメータ:
  - `postageFlag=1` — 送料込みのみ取得
  - `availability=1` — 在庫ありのみ取得
  - `NGKeyword` — 除外語をAPI側で処理（定期購入・健康訴求語）
  - `sort=-reviewCount` — レビュー数降順
  - `minPrice` / `maxPrice` — 価格帯指定
  - `genreId` — ジャンル限定

**除外処理は可能な限りAPI側に寄せること。**
取得後にフィルタすると30件取って数件しか残らず、候補が枯渇する。

ランキングAPIは「今売れているもの」、検索APIは「条件に合うもの」を担当させ、
`itemCode` で重複を除いてマージする。取得元は `source: "ranking" | "search"` で記録する。

### 4.3 レート制限

短時間に連続で叩くとエラーになる。**リクエスト間に1秒以上のsleepを入れること。**
ジャンル数が増えても直列実行とする。

### 4.4 対象ジャンル（設定ファイルで管理）

初期値：

| ジャンル | genreId | 料率（参考値） |
|---|---|---|
| 食品 | 100227 | 4% |
| スイーツ・お菓子 | 551167 | 4% |
| 水・ソフトドリンク | 100316 | 4% |
| コーヒー | 100356 | 4% |
| キッズ・ベビー・マタニティ | 100533 | 4% |
| キッチン用品・食器 | 558944 | 3% |
| 日用品雑貨 | 215783 | 3% |

`config/genres.json` に外出しし、UIまたはファイル編集で増減できるようにする。

**この料率はフォールバック値として扱うこと。**
APIが `affiliateRate` を返す場合はそちらを常に優先する。
設定ファイルの値は、APIが料率を返さなかった場合にのみ使用する。

## 5. データ構造

### 5.1 日次スナップショット `data/YYYY-MM-DD.json`

```json
{
  "fetchedAt": "2026-08-01T06:00:00+09:00",
  "genres": [
    {
      "genreId": "100227",
      "genreName": "食品",
      "commissionRate": 0.04,
      "items": [
        {
          "itemCode": "gourmetcoffee:10001806",
          "source": "ranking",
          "rank": 10,
          "prevRank": 14,
          "rankChange": 4,
          "isNew": false,
          "itemName": "送料無料 コーヒー豆 2kg 怒涛の珈琲豆...",
          "itemCaptionShort": "商品説明を300文字で切り詰めたもの",
          "catchcopy": "ショップの宣伝文（投稿文には転記しない）",
          "itemPrice": 5398,
          "hasPriceRange": false,
          "itemPriceMin": 5398,
          "itemPriceMax": 5398,
          "shopName": "グルメコーヒー豆専門！加藤珈琲店",
          "shopCode": "gourmetcoffee",
          "reviewCount": 43088,
          "reviewAverage": 4.53,
          "affiliateRate": 0.04,
          "isRateBoosted": false,
          "postageFlag": 1,
          "availability": 1,
          "pointRate": 10,
          "pointRateStart": "2026-08-04T20:00:00+09:00",
          "pointRateEnd": "2026-08-11T01:59:00+09:00",
          "priceEndTime": "2026-08-01T14:59:00+09:00",
          "itemUrl": "https://item.rakuten.co.jp/...",
          "imageUrl": "https://...",
          "estimatedReward": 324,
          "hotScore": 72,
          "excluded": false,
          "excludeReason": null,
          "draftComments": [
            { "angle": "手間削減", "text": "..." },
            { "angle": "ストック切れ回避", "text": "..." }
          ]
        }
      ]
    }
  ]
}
```

`data/index.json` に日付一覧を持たせ、PWA側が利用可能な日付を把握できるようにする。

**保存前の正規化ルール（必須）**

APIのレスポンスをそのまま保存しないこと。以下を必ず適用する。

| 処理 | 理由 |
|---|---|
| `itemPrice` `affiliateRate` `reviewAverage` を数値化 | 文字列で返るため。文字列比較は誤判定を招く |
| `affiliateRate` を100で割る（`"4.0"` → `0.04`） | パーセント表記のため |
| `itemCaption` を300文字で切り詰め、`itemCaptionShort` として保存 | 数千文字あり、日次JSONが肥大化する |
| `itemUrl` `shopUrl` から `rafcid` クエリを除去 | パブリックリポジトリへのアプリID露出を防ぐ |
| 日時文字列（`2026-08-04 20:00`）をISO 8601に変換 | JSTとして解釈し `+09:00` を付与する |
| 空文字の日時は `null` に統一 | `startTime` などは空文字で返ることがある |
| `mediumImageUrls[0].imageUrl` を `imageUrl` として1つだけ保持 | 配列全体は不要 |

`itemCaption` の全文が必要になるのは投稿文生成時のみ。
**必要なら取得時にAI用プロンプトを組み立てて保存し、原文は捨てる**方針とする。

### 5.2 セール日程 `data/sales.json`

**APIの `pointRateStartTime` / `pointRateEndTime` から自動推定する。**

実レスポンスで、複数の商品が同一の期間を持つことが確認できた。
例：`2026-08-04 20:00` 〜 `2026-08-11 01:59`（お買い物マラソンの期間と一致）。
また `2026-07-30 00:00` 〜 `2026-07-31 23:59` に集中する群も確認できた（Brand Day）。

**推定ロジック**

1. 全商品の `pointRateStartTime` / `pointRateEndTime` の組を集計する
2. 次の条件を**すべて**満たす組をセール期間の候補とする
   - **3件以上の異なる `shopCode` の商品**で出現する
   - `pointRateEndTime` が `9999-12-31 23:59` **でない**
   - 期間が60日以内
   - 終了日時が未来
3. `data/sales.json` に自動生成して書き出す

**2の条件は誤検出対策として必須。** 実データでは、澤井珈琲の商品5件が
`2025-10-28 14:00` 〜 `9999-12-31 23:59` という同一の期間を持っていた。
これはショップ独自の恒常的なポイント設定であり、セールではない。
**同一ショップ内の重複と、`9999` という無期限を示す値を必ず除外すること。**

実データで正しく検出できた例：
- `2026-07-30 00:00` 〜 `2026-07-31 23:59`（Brand Day）
- `2026-08-04 20:00` 〜 `2026-08-11 01:59`（お買い物マラソン）

```json
{
  "sales": [
    {
      "id": "auto-20260804",
      "name": "セール期間（自動検出）",
      "start": "2026-08-04T20:00:00+09:00",
      "end": "2026-08-11T01:59:00+09:00",
      "itemCount": 12,
      "source": "auto",
      "userLabel": "お買い物マラソン"
    }
  ]
}
```

**自動検出の名称は「セール期間（自動検出）」とし、正式名称は推測しないこと。**
ユーザーがUI上で `userLabel` を付けられるようにし、以降はそれを表示する。

手動でのセール追加も引き続き可能とする（`source: "manual"`）。
自動検出と手動登録が重複した場合は手動を優先する。

### 5.3 ユーザーローカル状態（localStorage / IndexedDB）

```json
{
  "schedule": { "gourmetcoffee:10001806": "2026-08-05" },
  "comments": { "gourmetcoffee:10001806": "編集済みの投稿文" },
  "posted":   { "gourmetcoffee:10001806": true },
  "settings": {
    "minPrice": 3000,
    "minReview": 500,
    "calendarMode": "scheduled"
  }
}
```

### 5.4 投稿ログ `posts`（分析の基盤）

「投稿済みにする」を押した時点で1レコードを確定保存する。
**この記録がないと後から分析ができない。売上データは6ヶ月遡れるが、
どの投稿文で投稿したかは自分で残さない限り復元できない。**

```json
{
  "posts": [
    {
      "postId": "uuid",
      "postedAt": "2026-08-05T07:12:00+09:00",
      "itemCode": "gourmetcoffee:10001806",
      "itemNameRaw": "送料無料 コーヒー豆 コーヒー 2kg 怒涛の珈琲豆 粉 (G500×4) 加藤珈琲/...",
      "shopName": "グルメコーヒー豆専門！加藤珈琲店",
      "genreId": "100227",
      "genreName": "食品",
      "itemPrice": 5398,
      "estimatedReward": 324,
      "angle": "ストック切れ回避",
      "firstLine": "コーヒー、切らしてから慌てて買っていませんか？",
      "firstLineLength": 24,
      "commentBody": "投稿した本文全文",
      "hashtags": ["#まとめ買い", "#共働き", "#コーヒー"],
      "usedAiGeneration": false,
      "duringSale": true,
      "saleId": "marathon-2026-08",
      "rankAtPost": 10,
      "rankChangeAtPost": 4
    }
  ]
}
```

**`itemNameRaw` は楽天APIが返した商品名をそのまま保存すること。**
成果データとの突合キーになるため、短縮・整形・トリムを一切行わない。

### 5.5 成果データ `results`（CSVインポート）

楽天アフィリエイトの注文明細レポートCSVを取り込んで保持する。

```json
{
  "results": [
    {
      "occurredAt": "2026-08-05T21:33:00+09:00",
      "itemNameRaw": "送料無料 コーヒー豆 コーヒー 2kg 怒涛の珈琲豆 粉 (G500×4) 加藤珈琲/...",
      "shopName": "グルメコーヒー豆専門！加藤珈琲店",
      "genreName": "食品",
      "salesAmount": 5398,
      "reward": 324,
      "rate": 0.04,
      "deviceType": "スマートフォン",
      "trackingId": "楽天ROOM",
      "status": "未確定",
      "matchedPostId": "uuid または null"
    }
  ]
}
```

**iOSの制約に注意。** ホーム画面に追加していないSafariのサイトは、
一定期間アクセスがないとスクリプトから書き込んだストレージが削除される場合がある。
以下の対策を必須とする。

- 初回起動時に「ホーム画面に追加」を促す案内を表示する
- 設定画面にJSONエクスポート／インポート機能を用意する
- 月1回程度エクスポートを促すリマインドを表示する

## 6. 抽出ロジック

### 6.1 除外条件（設定で閾値変更可）

| 条件 | 初期値 | 理由 |
|---|---|---|
| 価格が閾値未満 | 3,000円 | 報酬効率が悪い |
| レビュー件数が閾値未満 | 500件 | 信頼の担保がない |
| **`availability` が 0** | **必須** | **売り切れ。投稿しても成果にならない** |
| **`postageFlag` が 1** | **設定で切替** | **1が「送料別」。0が送料込みなので判定を逆にしないこと** |
| **`priceEndTime` が投稿予定日より前** | **必須** | **その価格が終了済み。実レスポンスでは数時間後に切れる商品が複数あった** |
| 商品名に定期購入・頒布会を示す語を含む | — | ROOMランクボーナスの対象外 |
| 健康効果を訴求する語を含む | — | 薬機法・健康増進法リスク |

**価格帯商品（`hasPriceRange: 1`）の価格判定は `itemPriceMax` を使うこと。**
実データでは、澤井珈琲の最上位商品が `itemPrice: 2788` / `itemPriceMax: 8888` だった。
`itemPrice` は最小構成の価格であり、これで足切りするとランキング1位の商品を取り逃す。
一方、想定報酬の算出は `itemPriceMin` を使い、保守的に見積もる。

**`itemPrice` と `itemPriceMin/Max` が食い違う場合がある。**
実データでは `itemPrice: 5506` に対し `itemPriceMin1/Max1` がともに `6478` という商品が存在した。
表示価格は `itemPrice` を正とし、乖離がある場合はUIに注意表示を出すこと。

**価格の有効期限は特に重要。** 実測データでは `endTime` が当日14:59に切れる商品が
ランキング上位に複数存在した。翌日に投稿すると価格が変わっている。

除外した商品は削除せず `excluded: true` と `excludeReason` を付けて残すこと。
閾値を緩めたときに再利用でき、除外理由の妥当性も後から検証できる。

健康訴求の判定キーワードは `config/ng-words.json` に外出しする。
初期値の例：痩せる、効く、改善、免疫、デトックス、血糖、血圧 など。

### 6.2 想定報酬と料率アップの判定

想定報酬は次の式で算出する。
**`affiliateRate` はパーセント表記の文字列（`"4.0"`）なので、数値化して100で割ること。**

```
rate = parseFloat(affiliateRate) / 100      // "4.0" → 0.04
       ※ 欠損時のみ config/genres.json の値をフォールバックとして使う

estimatedReward = min(itemPrice × rate, 1000)          // 通常報酬の上限1,000円
                + min(itemPrice × ROOMランクボーナス率, 1000)  // 初期値0.02（Aランク想定）
```

価格帯商品（`hasPriceRange: true`）は `itemPriceMin` を使って算出し、
UI上は「〜円から」と幅がある旨を明示する。

**ただし `affiliateRate` が 5% 以上の商品は料率アップ商品とみなし、
`isRateBoosted: true` を立てたうえで上限を適用しない。**
料率アップ商品は1商品1,000円の上限も同一ユーザー月3,000円の上限も対象外のため、
上限を適用すると報酬を過小評価する。

料率アップ商品は候補一覧で明示的にバッジ表示し、優先度を上げること。
通常商品は単価5万円で報酬が頭打ちになるのに対し、ここだけが青天井になる。

### 6.3 hotスコア

順位変動を主、規模を従として算出する。

```
hotScore =
    (前日順位 - 当日順位) × 5        // 上昇幅。新規ランクインは +30 とする
  + min(レビュー件数 / 1000, 20)      // 上限20点
  + 価格が5,000〜25,000円なら +15     // 報酬効率のスイートスポット
  + isRateBoosted なら +40            // 料率アップ商品を最優先で拾う
  + min(pointRate, 20)                // ポイント倍率。20倍で+20点
```

`pointRate` を加点に含めるのは、**買い手の購入判断に直結するため**。
実レスポンスでは通常時 `1`、キャンペーン中は `5〜20` と明確な差があった。
高倍率の期間は成約率が上がるとみて優先度を上げる。

検索API由来（`source: "search"`）の商品は順位を持たないため、
上昇幅の項を0として扱い、他の項のみで算出する。

係数は運用しながら調整する前提で、定数として1箇所にまとめておくこと。

### 6.4 前日比の計算

前日のJSONが存在しない場合（初回実行、Actions失敗時）は
`prevRank: null`, `isNew: true`, 上昇幅ボーナスなしとして扱い、エラーにしない。

## 7. 投稿予定日の割当

**優先順位は以下のとおり。**

1. その商品に `pointRateStart` があり、まだ到来していない場合はその開始日
2. 自動検出または手動登録されたセールの開始日で、抽出日以降の最も近いもの
3. 該当がなければ抽出日の翌日

商品ごとのポイント期間を最優先するのは、**その商品が最も売れやすい日を
商品自身が持っているため**。全体のセール日程より精度が高い。

ただし `priceEndTime` が投稿予定日より前になる場合は、割当を抽出日の翌日に戻し、
UI上に「価格の期限が近い」旨を表示する。

ユーザーはUI上で任意の日付に変更できる。手動変更した場合は自動再割当をしない。

## 8. 画面仕様

### 8.1 カレンダー画面（トップ）

- 月表示。左右スワイプで月移動
- **表示モード切替トグル：「投稿予定日」／「発見日」**
  - 投稿予定日モード：割り当てた日に商品が並ぶ（初期表示）
  - 発見日モード：抽出された日に商品が並ぶ
- セール期間は日付セルの背景に帯を表示し、セール名を短縮表示
- 各日付セルに候補件数のバッジ。投稿済みの件数は色を変える
- **料率アップ商品を含む日はセルにバッジを表示する**（見逃すと機会損失が大きいため）
- **セルを長押しすると、その日の想定報酬合計をポップアップ表示**
- 日付タップで日別リストへ

### 8.2 日別リスト画面

商品カードを hotScore 降順で表示。カードの内容：

- サムネイル画像
- 商品名（全文。長い場合は折りたたみ）
- 価格 / レビュー件数 / レビュー平均
- 順位と変動（例：10位 ↑4）、新規は「NEW」。検索API由来は「検索」と表示
- **実料率（`affiliateRate`）。5%以上は「料率UP」バッジを強調表示**
- **送料込みアイコン（`postageFlag`）**
- **在庫状態（`availability`）。在庫なしはカード全体をグレーアウト**
- 想定報酬（6.2の式で算出。上限適用の有無を区別して表示）
- 商品説明（`itemCaption`。初期は折りたたみ）
- 楽天商品ページへのリンク（外部ブラウザで開く）
- 投稿文エリア（テンプレート生成済み。編集可能。編集内容はローカル保存）
- ボタン：投稿予定日を変更／AI用プロンプトをコピー／投稿済みにする

**絞り込みチップを上部に配置する。**
「料率UPのみ」「送料込みのみ」「在庫ありのみ」「上昇中のみ」をタップで切り替えられること。

**同日に同一ショップの商品を投稿済みの場合は警告を表示する。**
クリック数はショップ単位でしか取得できないため、
同日同ショップの投稿が重なると投稿別のクリック分析ができなくなる。

「投稿済みにする」を押した時点で、5.4の投稿ログを1レコード確定保存する。
このとき `angle` `firstLine` `commentBody` `hashtags` を必ず含めること。
編集された投稿文がある場合は編集後の内容を保存する。

### 8.3 分析画面

CSVインポート後に集計結果を表示する。詳細は10章。

- 期間セレクタ（30日／90日／全期間）
- 角度別サマリ（表）
- ジャンル別サマリ（表）
- セール期間内外の比較
- 実料率の一覧（想定との乖離を強調表示）
- 未突合の成果一覧（手動で投稿と紐付けるUI）

### 8.4 設定画面

- 対象ジャンルの追加・削除・有効無効
- 除外条件の閾値（価格・レビュー件数）
- セール日程の追加・編集・削除
- **注文明細CSVのインポート**
- データのエクスポート／インポート
- キャッシュクリア

## 9. 投稿文の生成

### 9.1 テンプレート生成（アプリ内・無料）

Actions側で生成し、JSONに含める。角度は4種のうち商品に合うものを2つ選ぶ。

| 角度 | 使いどころ |
|---|---|
| 手間削減型 | ラベルレス、買い出し回数の削減 |
| ストック切れ回避型 | まとめ買い、消耗品 |
| コスパ比較型 | 1杯あたり、1本あたりの単価 |
| 季節型 | 夏＝麦茶・炭酸水、冬＝ホット・福袋 |

**生成ルール（重要）**

- 1行目は30〜35文字以内。ROOMのフィードではここしか表示されない
- 1行目は問いかけ／ターゲット指定／ベネフィットの名詞句のいずれか
- 商品名やブランド名から書き始めない
- 全体80〜150文字、1文ごとに改行、3行以内
- 末尾にハッシュタグ3〜4個
- **商品名に含まれる「最安値」「楽天1位」「半額」等の文言を投稿文に転記しない**
  （景表法上、表示責任は投稿者側にも及ぶため）

### 9.2 AI用プロンプトのコピー

外部APIは呼ばない。ボタンを押すと以下の形式のテキストをクリップボードにコピーし、
ユーザーがClaudeのチャットに貼り付けて高品質な投稿文を得る。

```
以下の商品について、楽天ROOMの投稿コメントを5案作ってください。

商品名：{itemName}
価格：{itemPrice}円
ショップ：{shopName}
レビュー：{reviewCount}件（平均{reviewAverage}）
商品説明：{itemCaption}

ターゲット：共働き世帯（子育て含む）
選定基準：まとめ買いで買い物回数を減らせるか

制約：
- 1行目は30〜35文字以内。ここしか表示されない
- 全体80〜150文字、1文ごとに改行、3行以内
- ハッシュタグ3〜4個
- 商品名の「最安値」「楽天1位」等は転記しない
- 実際には使用していないため、使用体験を装わない
- 5案は本文を近づけ、1行目のみ変える（ABテスト用）
```

## 10. 分析機能

### 10.1 前提：測れるものと測れないもの

| | 可否 | 備考 |
|---|---|---|
| 商品ごとの売上・報酬・料率 | ○ | 注文明細レポートに商品名単位で出る |
| ROOM経由かどうか | ○ | 計測IDが「楽天ROOM」なら該当。空欄は通常アフィリエイト |
| デバイス（PC/スマホ） | ○ | 注文明細に含まれる |
| 確定／未確定／破棄 | ○ | ステータスとして取得できる |
| ショップ単位のクリック数 | △ | ショップ別レポートのみ。商品には分解できない |
| **投稿ごとのクリック数** | **×** | 取得不可 |
| **表示回数・クリック率** | **×** | 取得不可 |

**クリック率が測れない以上、「1行目が良かったか」は直接測定できない。**
代替として「投稿した商品のうち何件が売れたか」を成約率とみなして角度別に比較する。
この設計上の割り切りを前提に指標を組むこと。

### 10.2 CSVインポート

楽天アフィリエイトの注文明細レポートをCSVで取り込む。

- **ダウンロードはPCからのみ可能。** スマホからは実行できないため、
  分析はPCで月1〜2回行う運用とする。アプリはファイル選択によるインポートのみ対応する。
- 過去データは最大6ヶ月前まで。**それ以前は遡れないため、取り込んだCSVは
  アプリ側で永続保持する**こと（インポート済みデータを上書き削除しない）。
- 同一レコードの重複登録を防ぐため、`occurredAt` + `itemNameRaw` + `salesAmount`
  の組でユニーク判定する。
- 文字コードはShift_JISの可能性があるため、UTF-8とShift_JISの両方を試行する実装にする。
- 列構成は変更される可能性があるため、**ヘッダ行を読んで列名でマッピングする。
  列位置の決め打ちは禁止。**

### 10.3 突合ロジック

投稿ログ（5.4）と成果データ（5.5）を `itemNameRaw` の完全一致で突合する。

- 完全一致しない場合、ショップ名一致 + 商品名の前方一致（30文字）でフォールバック
- それでも一致しない成果は `matchedPostId: null` のまま「未突合」として残す
- 未突合の成果は分析画面に一覧表示し、手動で投稿と紐付けられるようにする
- **未突合を自動で捨てないこと。** note等ROOM以外の流入や、
  過去投稿からの遅れて発生した成果が含まれるため、実態の把握に必要。

同一商品を複数回投稿している場合は、成果発生日時に最も近い直前の投稿に紐付ける。

### 10.4 集計指標

**層1：角度別（最重要）**

| 指標 | 算出 |
|---|---|
| 投稿数 | 角度別の投稿件数 |
| 成約商品数 | その角度で投稿した商品のうち成果が発生した件数 |
| 成約率 | 成約商品数 ÷ 投稿数 |
| 1投稿あたり報酬 | 報酬合計 ÷ 投稿数 |

1行目の文字数帯（〜25／26〜35／36〜）でも同じ集計を出す。

**層2：タイミング**

- セール期間内投稿と期間外投稿の成約率・1投稿あたり報酬の比較
- 曜日別、投稿時刻帯別の成果件数
- デバイス比率（スマホ比率が高いほど1行目の短さが効く裏付けになる）

**層3：ジャンルと料率の検証**

| 指標 | 算出 | 目的 |
|---|---|---|
| 実料率 | 報酬 ÷ 売上金額 | 想定料率（3%/4%）の答え合わせ |
| 想定との乖離 | 実報酬 − 想定報酬 | ジャンル選定の前提が正しいか検証 |
| 破棄率 | 破棄件数 ÷ 全件数 | 予約商品の発送遅れ等による失注の可視化 |
| ROOM経由比率 | ROOM経由報酬 ÷ 全報酬 | 外部流入を始めた際の効果測定 |

**実料率は設定画面のジャンル料率にフィードバックできるようにする。**
初期値は推定値であり、実データで上書きされるべき値として扱う。

### 10.5 表示上の注意

- サンプル数が少ない段階での成約率は誤差が大きい。
  **投稿数が10件未満の区分は数値を出さず「データ不足」と表示すること。**
  少数のデータで角度を絞り込むと、判断を誤ったまま固定化する。
- 報酬は確定前の値を含むため、確定／未確定を区別して表示する。

## 11. 開発フェーズ

| Phase | 内容 | 完了条件 |
|---|---|---|
| 1 | Actionsで日次JSON生成＋最小PWA（リスト表示のみ）＋**投稿ログの記録** | iPhoneで当日の候補が見られ、投稿が記録される |
| 2 | カレンダー、投稿予定日割当、セール表示 | 日付タップで候補が出る |
| 3 | 投稿文テンプレート、AI用プロンプトコピー、投稿済み管理 | 投稿までの動線が完結する |
| 4 | CSVインポート、突合、分析画面 | 角度別の成約率が見える |
| 5 | 実料率のフィードバック、抽出条件の自動調整提案 | 数字に基づいて条件を更新できる |

**投稿ログの記録はPhase 1に含めること。**
分析画面はPhase 4で構わないが、記録が始まっていなければPhase 4の時点で
分析対象のデータが存在しない。成果データは6ヶ月遡れるが、投稿ログは遡れない。

**Phase 1の時点で日次JSONの構造を確定させること。**
順位変動の計算には過去データの蓄積が必要で、後から項目を足しても過去分は埋まらない。
`itemCode` を主キー、日付との組を一意キーとする設計を最初から徹底する。

## 12. 実装前に確認すること

**確認済み（2026-07-31）**

- アプリIDはUUID形式。楽天ウェブサービスで発行済み
- **`genreId` は正しく機能する。** テストフォームのパラメータ欄に指定すること
- `affiliateRate` `postageFlag` `availability` `pointRate` `catchcopy` はすべて取得可能
- `affiliateRate` はパーセント表記の文字列（`"4.0"`）
- **`postageFlag` は 0 が送料込み、1 が送料別**
- 返却件数は30件、配列は順位の降順
- `pointRateStartTime` / `pointRateEndTime` からセール期間を推定可能（誤検出対策は6章参照）
- 除外条件を実データ（コーヒー30件）に適用したところ12件が通過。閾値は妥当

**未解決・要確認**

1. `formatVersion=2` で `Item` のネストが外れるか、型が改善されるかを確認する
2. 商品検索API（`IchibaItem/Search`）でも同じフィールドが返るか確認する
3. リポジトリを公開／非公開のどちらにするか決める（無料プランはPublicのみPages利用可）
4. GitHub Actionsのcron実行が遅延することがある点を許容する（数十分の誤差）
5. iOSでホーム画面に追加した状態でlocalStorageが保持されるか、実機で確認する
6. 7ジャンル×30件の日次JSONが、切り詰め後にどの程度のサイズになるか実測する
6. **注文明細レポートCSVを1度ダウンロードし、実際の列名・文字コードを確認する。**
   本書の成果データ構造は推定を含むため、実ファイルを正としてマッピングを組むこと
7. 楽天アフィリエイトのレポート項目は変更されうるため、
   列名が想定と異なる場合にエラーで落とさず、警告表示して手動マッピングできる設計にする

# 楽天ROOM 投稿支援ツール 追加要件（v1.2）

既存実装への差分のみ。背景は 2026/8/18 時点の投稿ログ47件の実測。

## 0. 現状の問題

| 項目 | 実測 | 問題 |
|---|---|---|
| いいね数・クリック数・売上 | **1列もない** | 何が効いたか永久に判定できない |
| ヘッダー型 | 47件中27件が空欄 | 分類が機能していない |
| 角度 | 13種類の表記ゆれ | 「セール速報」と「セール速報型」が併存 |
| ヘッダー本文 | タグや箇条書きが混入 | 1行目が正しく保存されていない |
| オリジナル写真 | **0件** | Bランク未達＝報酬率4%に据え置き |

分類が入力できない直接の原因は、**プロンプト側の分類名が3回変更され、
アプリの選択肢が古い版のまま残っている**こと。
「近いものを選ぶ」運用になっており、分析に使えないデータが蓄積している。

---

## 1. 分類の再設計（最優先）

### 1.1 「角度」列を廃止する

ヘッダー型と情報が重複している。47件を7分類に割ると1区分あたり数件となり、
「投稿数10件未満の区分は数値を出さない」という既存方針に照らして
最初から分析不能である。**1軸に統合する。**

### 1.2 「ヘッダー型」を4分類に固定

選択肢を以下に差し替える。**この名称は今後変更しない。**

| 値 | 判定基準（実際に投稿した1行目が何から始まるか） |
|---|---|
| `共感課題型` | 読み手の悩み・状況 |
| `お得条件型` | 割引率・価格・期限 |
| `称号実績型` | 受賞歴・ランキング・実績 |
| `商品特徴型` | 容量・加工・スペック |
| `判定不可` | 上記のどれにも当てはまらない |

**`判定不可` を必ず選択肢に含めること。**
これがないと「近いものを選ぶ」運用になり、偽のデータが蓄積する。
空欄で保存できないようにし、代わりに `判定不可` を選べるようにする。

### 1.3 「投稿済み」操作時に分類を必須入力にする

現在は投稿後に分類を入力する動線がなく、27/47件が空欄になっている。
**「投稿済みにする」ボタンを押した時点で、
分類の選択ダイアログを表示し、未選択では確定できないようにする。**

選択は4＋1のボタンを1タップ。それ以上の入力は求めない。

### 1.4 「ヘッダー本文」列の保存内容を修正

現状、ハッシュタグや箇条書きが混入している行が複数ある。
**投稿本文の1行目（最初の改行まで）のみを保存する。**
空行が先頭にある場合は、最初の非空行を1行目とみなす。

### 1.5 過去47件の扱い

ヘッダー本文が汚れているため、遡っての再分類は行わない。
**過去分は分析対象外とし、この改修以降のデータで集計する。**
既存レコードには `分類方式: v1` のフラグを立て、
新規レコードは `分類方式: v2` として区別する。

---

## 2. 成果データの取得

### 2.1 いいね数の手入力欄（新規）

ROOMにAPIはなく、画面もJavaScript描画のため自動取得できない。
**投稿済みリストに数値入力欄を1つ追加する。**

- 週1回、my ROOMを見て直近の投稿のいいね数を転記する運用を想定
- 入力日時も併せて記録する（`いいね数`, `いいね計測日`）
- 同一投稿に複数回入力できるようにし、履歴を保持する
　（投稿直後と1週間後で伸び方が違うため、初速の判定に使う）

### 2.2 注文明細CSVのインポート（既存要件の実装）

要件定義書v1.1の10章に仕様済み。**未実装のため、ここで優先度を上げる。**
月5万円という目標に対して、いいね数より直接的な指標である。

- 楽天アフィリエイトの注文明細レポートCSVを取り込む
- 商品名で投稿ログと突合する
- 計測IDが「楽天ROOM」のものだけを抽出できるようにする

### 2.3 集計画面に追加する指標

分類がv2で10件以上溜まったら表示する（それ未満は「データ不足」と表示）。

| 指標 | 算出 |
|---|---|
| ヘッダー型別の平均いいね数 | いいね合計 ÷ 投稿数 |
| ヘッダー型別の成約率 | 成果発生した商品数 ÷ 投稿数 |
| 購入済み商品の有無による差 | 後述の実購入フラグで分割 |

---

## 3. 実購入フラグの追加（新規）

### 3.1 背景

複数の調査が一致して「一人称の体験談が高エンゲージメントの軸」と指摘している。
現在は購入していないため体験談が書けず、オリジナル写真も0件で、
Bランクにも到達していない（＝報酬率が4%のまま、本来の6%の3分の2）。

リーチ枠（1,000〜3,000円）の商品を月数点購入すれば、
**体験談・オリジナル写真・Bランク条件が同時に解決する。**

### 3.2 実装

商品カードに `購入済み` のチェックを追加する。

- チェックが入った商品は、AI用プロンプトのコピー内容に
　`【この商品は購入済み。一人称の体験談を書いてよい】` の1行を追加する
- 購入済み商品の投稿には `#オリジナル写真` タグの付与を促す
- 集計画面で「購入済み投稿」と「未購入投稿」のいいね数・成約率を比較表示する
　→ 購入する価値があるかどうかを、数字で判断できるようにする

---

## 4. ハッシュタグ支援（新規）

### 4.1 背景

実測：#鶏なんこつ唐揚げ（ニッチ）で1位、#無洗米（中規模）で中位、
#お取り寄せスイーツ（ビッグ）で圏外。
フォロワー486人の段階では、ビッグタグでは露出が得られない。
**ニッチタグが唯一の露出経路。**

### 4.2 実装

商品名からニッチタグ候補を自動生成し、コピー可能な形で表示する。

- 商品名から名詞を抽出し、`#` を付けた候補を出す
　例：「骨取りサバ切身 1kg」→ `#骨取りサバ` `#サバ切身` `#骨取り魚`
- 修飾語（送料無料、訳あり、最安値、クーポン等）は候補から除外する
- 生成した候補を「ニッチ」、既存の頻出タグを「中規模／ビッグ」として
　3層に分けて表示し、それぞれから選べるようにする

### 4.3 タグの記録方法を統一する

現状、タグが「ハッシュタグ列」と「投稿本文列」のどちらかに入っており統一されていない。
**投稿本文からタグを自動抽出し、`ハッシュタグ` 列に一元化して保存する。**
`タグ数` も本文からの実測値で再計算する（現在0と記録されている行がある）。

---

## 5. 実装順序

| 優先 | 項目 | 理由 |
|---|---|---|
| 1 | 1章（分類の4分類化・必須入力・1行目保存） | これがないと以降のデータが全て無駄になる |
| 2 | 3章（実購入フラグ） | 実装は軽い。判断材料を作るため早く始める |
| 3 | 2.1（いいね数入力欄） | 週1回5分の運用で成立する |
| 4 | 4章（ハッシュタグ支援） | 露出に直結するが、手動でも代替できる |
| 5 | 2.2（注文明細CSV） | 実装が重い。1か月分の投稿が溜まってからでよい |

**1章だけで、以降の分析可否が決まる。** ここを最優先で反映すること。

---

## 6. 検証時に確認すること

- 「投稿済み」時の分類ダイアログが、投稿の妨げになるほど煩わしくないか
　→ 1タップで済むか。2タップ以上必要なら簡略化する
- ヘッダー本文の1行目抽出が、箇条書きやタグを拾っていないか
- ニッチタグの自動生成が、意味をなさない断片を出していないか
　（「#1kg」「#送料」など。除外語リストの調整が必要）
- 購入済みフラグを立てた投稿で、AIが実際に体験談を書いているか
